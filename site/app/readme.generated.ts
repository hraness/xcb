// Generated from ../README.md by scripts/sync-readme.ts. Do not edit.
export const readmeTitle = "xcb";
export const readmeLead = "Excalibur (`xcb`) brings your coding-agent accounts, model choices, sessions, and usage into one local terminal workspace. Choose an account, work on your project, and pick up where you left off without changing interfaces.";
export const readmeHtml = "<h1 id=\"xcb\">xcb</h1>\n<p>Excalibur (<code>xcb</code>) brings your coding-agent accounts, model choices, sessions,\nand usage into one local terminal workspace. Choose an account, work on your\nproject, and pick up where you left off without changing interfaces.</p>\n<p>The native Rust app is a source preview for supported Claude, Codex, and Devin\nruntimes. It includes workspace file tools, an isolated Linux command runner,\ncustomizable panes, and a separate application API. Provider support and\nexecution boundaries are explicit; it is not a replacement for every feature\nof the original provider tools.</p>\n<p><a href=\"https://xcb.sh\">Project site</a> · <a href=\"https://xcb.sh/docs/getting-started\">Getting started</a> ·\n<a href=\"https://xcb.sh/compare\">Compare tools</a> · <a href=\"https://github.com/hraness/xcb\">Source</a> ·\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/application-api.md\">Application API</a> · <a href=\"https://github.com/hraness/xcb/blob/main/docs/compatibility.md\">Compatibility reference</a> · <a href=\"https://github.com/hraness/xcb/blob/main/CONTRIBUTING.md\">Contributing</a></p>\n<h2 id=\"readiness\">Readiness</h2>\n<p><strong>xcb is not yet a daily-driver replacement for Codex, Claude Code, and Devin.</strong>\nThe native broker lists, reads, searches, and writes workspace files, creates\ndirectories, and removes or renames regular files with revision checks. The\nsource also includes an isolated Linux command runner for tests and builds on\nmacOS ARM64. The current backend passed its 12-case VM boundary suite, including\nfiltered Git inspection, public dependency fetching, and offline Cargo/Bun use\nfrom immutable caches. Installed Claude and Codex coding workflows passed on\nmacOS ARM64: an expected test failure, exact repair, passing test, and filtered\nGit status, with joined processes and settled effects. This evidence covers the\ntested accounts and admitted builds. Devin's credential-free boundary checks\nare separate from authenticated coding acceptance. See the <a href=\"https://github.com/hraness/xcb/blob/main/docs/command-runner.md\">command runner contract</a>\nfor setup, supported boundaries, and current limits.</p>\n<table>\n<thead>\n<tr><th>Provider</th><th>Native Rust CLI</th><th>TypeScript compatibility CLI</th></tr>\n</thead>\n<tbody>\n<tr><td>Claude</td><td>Installed coding workflow verified on macOS ARM64 with the tested account; Linux remains an execution candidate after sign-in, binary admission, and confinement checks</td><td>Execution candidate, subject to its own admission and confinement checks</td></tr>\n<tr><td>Codex</td><td>Native app-server on macOS for exact build <strong>0.155.0-alpha.2.6</strong>; authenticated broker and installed coding workflow acceptance passed on macOS ARM64 with the tested account</td><td>Discovery only; managed task execution gated on host qualification</td></tr>\n<tr><td>Devin</td><td>Native ACP candidate on macOS for exact builds <strong>3000.11.1</strong> and <strong>3000.10.31</strong>; both passed credential-free boundary checks; model availability is checked against the connected account's fresh catalog at launch</td><td>ACP implementation exists; task execution disabled pending exact-runtime qualification</td></tr>\n</tbody>\n</table>\n<p>A successful <code>doctor</code> or a visible model does not prove a working coding session.\nThe current Devin CLI can be authenticated and can return its model catalog, but\nthat provider login is separate from XCB's explicit credential import and from\nan admitted coding turn. Devin validates the selected model against the\nconnected account's fresh catalog before each turn. The September 20, 2026\nquota result is historical evidence, not a statement of current availability.\nNative <code>doctor</code> reports <code>metadata pin only</code> for unqualified providers. Codex and\nDevin candidates require the checked executable digest as well as the version;\nother builds and their Linux execution paths remain unavailable. Automated\nfixtures check boundaries; they do not establish authentication, service\nreliability, or real-model task quality. The TypeScript compatibility CLI's\nCodex and Devin task routes remain unqualified and disabled.</p>\n<h2 id=\"native-xcb\">Native xcb</h2>\n<p>The source build is the current installation path. As of September 19, 2026,\nGitHub's latest published release is <strong>AgentMixer v0.3.0</strong>, with an AgentMixer\npackage archive. No native xcb release or <code>@hraness/xcb</code> npm package is published.\nSource version 0.4.0 is not a published release. Check the\n<a href=\"https://github.com/hraness/xcb/releases\">release assets</a> before downloading.</p>\n<h3 id=\"install-from-source\">Install from source</h3>\n<p>Requires Git, the pinned Rust <strong>1.97.1</strong> toolchain, and platform build tools.\nClaude's supported execution boundary is macOS Seatbelt or Linux with a working,\nadmitted <code>bwrap</code> configuration. The native Codex and Devin candidates currently\nrequire macOS Seatbelt; unsupported confinement fails closed.</p>\n<pre><code class=\"language-sh\">git clone https://github.com/hraness/xcb.git\ncd xcb\nrustup toolchain install 1.97.1 --profile minimal\n./scripts/install-native.sh\nexport PATH=&quot;$HOME/.local/bin:$PATH&quot;\nxcb --version\nxcb --help\n</code></pre>\n<p>The installer builds with the lockfile and installs <code>~/.local/bin/xcb</code>.\n<code>XCB_INSTALL_PREFIX</code> changes the prefix. Both the old TypeScript CLI and the\nnative CLI use the name <code>xcb</code>; use <code>command -v xcb</code> to check which one is active.\nThe installer also records a private install manifest under the prefix and\nkeeps the exact installer beside the binary, so later upgrades use the same\nverified path.</p>\n<h3 id=\"updates-and-global-operation\">Updates and global operation</h3>\n<p>The native binary is a user-global install when it lives in <code>~/.local/bin</code> and\nthat directory is on <code>PATH</code>. XCB follows an OpenCode-style policy: <code>notify</code> is\nthe default, <code>auto</code> installs only an exact stable release with its checksum,\nand <code>disable</code> turns checks off. A macOS LaunchAgent runs the check once a day\nwhen you enable it; it never reads project settings or updates from <code>main</code>.</p>\n<pre><code class=\"language-sh\">xcb update check\nxcb update enable --policy notify   # check daily and tell you when a release exists\nxcb update enable --policy auto     # check daily and install verified releases\nxcb update status\nxcb upgrade                         # install the latest verified native release\nxcb update disable\n</code></pre>\n<p>There is currently no published native xcb release, so the updater fails closed\nand leaves the source-installed binary alone until the first verified\n<code>xcb-&lt;version&gt;-&lt;platform&gt;-&lt;arch&gt;.tar.gz</code> release is available. After any\nreplacement, restart open terminals and rerun <code>xcb doctor</code>; provider and\napplication qualification is bound to the exact installed executable bytes.</p>\n<p>Managed supervisors record their exact executable identity. When that binary\nis replaced, a current supervisor stops starting new turns, retains custody of\nits active workers until they settle, then exits. Queued tasks and tasks waiting\nfor input remain saved. Wait for that exit, restart the terminal, refresh provider\npins with <code>xcb doctor</code>, and reopen the control conversation to continue.\nA different running build produces an explicit supervisor-version error.\nLegacy supervisors without an identity record need their exact process verified\nand stopped after active workers settle; a saved PID or a deleted lock file is\nnot a safe replacement for that verification.</p>\n<p>Managed records can gain fields that older source builds reject. Restart old\nclients and supervisors before using updated managed state, and retain that state\nduring an installation rollback. Replacing the binary does not migrate provider\nsessions or establish fresh live acceptance across all three providers.</p>\n<h3 id=\"first-managed-conversation\">First managed conversation</h3>\n<p>Install an admitted Claude Code binary (major 2, version 2.1.268 or newer).\nxcb performs its own account sign-in below; it does\nnot silently import your existing provider login. Replace <code>&lt;account-id&gt;</code> below\nwith the generated ID printed by <code>accounts add</code> or <code>accounts import-*</code> (also\nlisted by <code>xcb accounts</code>). Account names come from observed provider identities;\ncustom labels are not accepted.</p>\n<pre><code class=\"language-sh\">xcb accounts add claude --plan Max\nxcb doctor --provider claude\nxcb accounts login &lt;account-id&gt;\nxcb accounts refresh &lt;account-id&gt;\nxcb models\nxcb --cwd /absolute/path/to/your/project\n</code></pre>\n<p>Plain <code>xcb</code> opens a new persistent control conversation. Prompts become durable\nmanaged tasks routed through admitted Codex, Claude, or Devin sessions; closing\nthe terminal detaches without cancelling them. Open another terminal for an\nindependent conversation over the same task swarm, use <code>/tasks</code> to inspect work,\nor <code>/sessions</code> to switch control conversations. Ordinary prompts create new work;\nwhen one task in the conversation asks for input, the next reply answers it.\nUse <code>new task: …</code> to explicitly start separate work. Independent workspaces can\nrun concurrently; tasks in the same workspace run one at a time. Say\n<code>cancel &lt;task-id&gt;</code> to request cancellation and inspect <code>/tasks</code> for settlement.</p>\n<p>The Rust supervisor owns scheduling and deterministic safety decisions. ALGAL\nrecords bounded transition receipts; it does not infer permissions, establish\nprovider qualification, or replace the supervisor’s execution policy.\n<code>xcb tasks verify &lt;task-id&gt;</code> replays that task’s local receipt chain and checks\nit against the current record; it does not attest provider claims or real-world\noutcomes.</p>\n<p>Managed routing first filters for qualified, credentialed, idle, quota-usable\naccounts. It then ranks bounded model candidates by task fit, fresh remaining\nusage, relative quality/cost/latency and Pareto layer, while preserving explicit\nprovider requests and soft workspace-learned preferences. Official temporary\noffers are cached as expiring observations. They do not prove account entitlement\nor reduce a route’s estimated cost without that evidence. They never activate an\nunqualified provider or survive stale terms. Managed Claude, Codex and Devin workers share\n<code>xcb_swarm_status</code>, <code>xcb_message_list</code> and <code>xcb_message_send</code> for durable,\nworkspace-scoped cross-provider coordination.</p>\n<p>A settled authentication failure marks that account as requiring reconnection\nand excludes it from new task routes, including after restart. Other eligible\naccounts still respect the requested provider. Successful sign-in or an explicit\nimport with changed credential material clears the block; catalog refresh and\nreimporting the same credentials do not. Older failure records have no credential\ngeneration binding, so an upgraded account may need one new bounded attempt to\nestablish this block.</p>\n<p><code>--plan</code> is a display label; it does not verify your subscription. Complete the\nbrowser sign-in when prompted. <code>accounts refresh</code> probes supported model and\nusage metadata. Unknown or stale usage percentages remain unknown. A proven\nClaude account-wide quota exhaustion stays blocked until its reported reset,\neven when its percentage has gone stale. The account list shows a retry estimate;\nsee <a href=\"https://github.com/hraness/xcb/blob/main/docs/quota-routing.md\">quota routing</a> for the scope and credential binding.\nFor direct sessions, select a model by copying its full observed key from\n<code>xcb models</code> and running <code>xcb models default &lt;key&gt;</code>. Managed tasks choose among\neligible routes; begin a task with <code>Use Claude</code>, <code>Use Codex</code>, or <code>Use Devin</code> to\nrequire that provider.</p>\n<p>If discovery finds the wrong binary, use\n<code>xcb doctor --provider claude --executable /absolute/path/to/claude</code>.\nThe pin binds executable bytes and version. After upgrading xcb, restart open\nxcb terminals and rerun <code>doctor</code>. A process started from the old binary cannot\nadopt the replacement binary's pin, and older clients refuse new run records\nwhose credential-custody format they do not understand.\nAn account, metadata pin, or model listing cannot activate an unqualified adapter.</p>\n<h3 id=\"connect-codex-on-macos\">Connect Codex on macOS</h3>\n<p>Use the exact admitted <strong>0.155.0-alpha.2.6</strong> build. Authenticated broker\nread/write/read and installed coding-workflow acceptance passed on macOS ARM64\nwith the tested account; this does not qualify arbitrary provider versions or\nthe separate application API. xcb supervises the official\nCLI's ChatGPT device sign-in in a private profile:</p>\n<pre><code class=\"language-sh\">xcb doctor --provider codex\nxcb accounts add codex --plan ChatGPT\nxcb accounts login &lt;account-id&gt;\nxcb accounts refresh &lt;account-id&gt;\nxcb models\n</code></pre>\n<p>Follow the device sign-in instructions shown in the terminal. Alternatively,\ncopy one existing ChatGPT credential into a new xcb account by selecting its\nprivate <code>auth.json</code> explicitly:</p>\n<pre><code class=\"language-sh\">xcb accounts import-codex --source /absolute/path/to/auth.json\nxcb accounts refresh &lt;account-id&gt;\n</code></pre>\n<p>The source file is preserved. Import does not copy provider configuration,\nplugins, sessions, or transcripts. API-key credentials are not accepted by this\nroute. Refreshed ChatGPT credentials are persisted only after the owned provider\nprocess has joined.</p>\n<h3 id=\"connect-devin-on-macos\">Connect Devin on macOS</h3>\n<p>Use the exact admitted <strong>3000.11.1</strong> build; <strong>3000.10.31</strong> remains admitted.\nBoth passed credential-free native boundary checks; authenticated coding\nacceptance requires separate evidence for the account, model, and build.\nModel availability is checked against the connected account's fresh catalog at\nlaunch. xcb preserves an unknown quota reset as unknown. Sign in through the\nprovider CLI, then explicitly select its\n<code>credentials.toml</code> to create a private xcb account:</p>\n<pre><code class=\"language-sh\">devin auth login\nxcb doctor --provider devin\nxcb accounts import-devin --source /absolute/path/to/credentials.toml\nxcb accounts refresh &lt;account-id&gt;\nxcb models\n</code></pre>\n<p>The source file and provider sessions are preserved. xcb copies only the\ncredential for the supported provider endpoints. To update just the catalog,\nuse <code>xcb models refresh devin --account &lt;account-id&gt;</code>; Devin discovery requires\nan explicitly connected account. Native Devin currently uses fixed ACP model\nchoices. Adaptive and Fusion catalog representations in the compatibility\npackage do not establish native support.</p>\n<p>For either provider, copy a full matching model key from <code>xcb models</code> and set it\nwith <code>xcb models default &lt;key&gt;</code> for direct sessions. Select the account with\n<code>xcb accounts default &lt;account&gt;</code> for new direct sessions, or pass\n<code>--account &lt;account&gt; --model &lt;key&gt;</code> to <code>xcb run</code>. Rerun <code>doctor</code> after a provider\nupgrade; a new version is not automatically admitted.</p>\n<h3 id=\"isolated-tests-builds-and-git\">Isolated tests, builds, and Git</h3>\n<p>Project commands use an explicitly provisioned Linux VM through <code>workspace_exec</code>.\nFollow the <a href=\"https://github.com/hraness/xcb/blob/main/docs/command-runner.md#setup-and-admission\">command runner setup</a>\nfrom the same source checkout as the installed native CLI. Commands run offline\nagainst a staged workspace; host dependencies, credentials, and build products\nare excluded. Native macOS and Xcode builds are unavailable. The explicit\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/command-runner.md#dependencies-and-git\">public dependency preparation frontend</a>\npassed the current VM boundary suite, including rejection of a cache after its\nmanifest changed. Installed Claude and Codex coding workflows passed on macOS\nARM64 with the tested accounts; other repositories and toolchains still need\ntheir own checks.</p>\n<p>The Git projection is limited to filtered, read-only HEAD and index data for\nstatus and diffs. Original history, remotes, and hooks are omitted; commit and\npush workflows are unavailable. Publication checks file revisions and replaces\neach file atomically; it is not a transaction across every changed file. Failed,\ncancelled, or uncertain command state is retained. Successfully published and\ndurably settled commands remove their verified input snapshot.</p>\n<h3 id=\"application-integration\">Application integration</h3>\n<p>The <a href=\"https://github.com/hraness/xcb/blob/main/docs/application-api.md\">application API</a> provides bounded, ephemeral\ninference with no tools or hooks. It requires evidence for the exact XCB binary,\nprovider, account and model before accepting application traffic.\n<a href=\"https://github.com/hraness/textbutler\">TextButler</a>, an MIT-licensed reference\napplication, keeps its contact access and messaging approval in its own host.\nSign-in and a successful <code>doctor</code> alone do not qualify the application route.</p>\n<h3 id=\"everyday-commands\">Everyday commands</h3>\n<pre><code class=\"language-sh\">xcb --cwd /absolute/path/to/your/project       # new control conversation\nxcb conversations                                # resumable control conversations\nxcb chat --resume &lt;conversation-id&gt;\nxcb tasks                                        # global managed task swarm\nxcb tasks verify &lt;task-id&gt;                       # verify local transition receipts\nxcb tasks messages &lt;task-id&gt;                     # durable cross-provider mailbox\nxcb offers --refresh                             # refresh official expiring offers\nxcb models tiers --task &quot;fix a race&quot;             # inspect Pareto layers\nxcb models route --task &quot;fix a race&quot;             # preview the eligible smart route\nxcb --cwd /absolute/path/to/your/project run --account &lt;account-id&gt; -p &quot;Explain this repository&quot;\nxcb sessions                                     # direct provider sessions\nxcb resume                                       # latest direct provider session\nxcb resume &lt;session-id&gt;\nxcb accounts\nxcb config\nxcb plugins\nxcb panes\nxcb doctor\nxcb completions zsh &gt; /path/to/completions/_xcb\n</code></pre>\n<p><code>xcb chat --resume</code> reopens a control conversation; <code>xcb resume</code> opens a saved\ndirect provider session and its workspace. Neither is a headless continuation\ncommand. <code>/help</code> lists terminal commands. Conversations, tasks, provider\nsessions, and credentials live in the private native state root\n<code>~/.local/share/xcb</code>; <code>--state /absolute/path</code> or <code>XCB_STATE</code> overrides it.\nThe compatibility CLI uses <code>~/.xcb</code> instead. Do not point both implementations\nat the same state directory. <code>xcb --json run</code> includes the native session ID\nin its result so it can be reopened with <code>xcb resume &lt;session-id&gt;</code>.</p>\n<p>One provider turn has a 30-minute default deadline, including initialization.\nThe <code>turn_timeout_ms</code> setting in the private state root's <code>config.json</code> accepts\n1,000–3,600,000 milliseconds (one second to 60 minutes); <code>xcb config</code> displays the\neffective configuration. Older configurations that omit it use the default.\nCancellation remains available before the deadline, and automatic continuation\nhas its own separate limits.</p>\n<p>Control conversations are concurrent and share one durable task supervisor.\nEach account still owns at most one active provider turn, and one workspace can\nhave only one admitted writer even when different accounts or terminals are\navailable. Independent workspaces and accounts can run concurrently. Managed\ncancellation may be requested from the task’s originating conversation or by an\nexplicit task ID/title elsewhere; direct provider-session cancellation remains\nowned by its terminal. Ctrl-C and SIGTERM request bounded cleanup for a headless\nrun. <code>xcb run</code> reports success only for a completed, joined, settled idle result.</p>\n<p>Cancellation joins the owned process before releasing custody. If <code>doctor</code>\nreports an unsettled run, inspect <code>xcb recover</code> and the process state; an expired\nlease or a quiet terminal is not proof that the provider stopped. Recovery is\nan explicit operation, not a reason to delete state or lock files.</p>\n<h3 id=\"optional-behavior\">Optional behavior</h3>\n<p>Auto-continue and Gobstopper context management default on with bounded\ncontinuation and settled-boundary checks. Disable either with\n<code>xcb plugins disable auto-continue</code> or <code>xcb plugins disable gobstopper</code>.\nLocal usage measurement stays local. aiCharts upload is unavailable; local\nexports, external judgment, and executable hooks require separate opt-in.\nPanes are presentation data and cannot grant execution authority.</p>\n<p>The optional judge uses TypeSafe System One (<code>jev-latest</code>) to advise routing,\ncontinuation, and context retention. It sends bounded task/response context to\nthat service; old tool-result bodies are excluded from compaction advice.\nA judge cannot qualify a provider or bypass deterministic safety gates.\nStore a key through stdin on macOS or Linux, then explicitly enable it:</p>\n<pre><code class=\"language-sh\">xcb judge token &lt; /secure/path/to/judge-key\nxcb judge status\nxcb judge enable\nxcb judge test\n# Later:\nxcb judge disable\nxcb judge logout\n</code></pre>\n<p>The input file is an existing private credential file, not a command-line key.\nKeys are vaulted mode-0600 outside the workspace. <code>XCB_JEV_API_KEY</code> or\n<code>TYPESAFE_API_KEY</code> may supply a key via the environment. Custom endpoints require\nan explicit environment key; the vaulted key remains bound to System One.</p>\n<h2 id=\"migrating-from-agentmixer\">Migrating from AgentMixer</h2>\n<p>The native command imports <strong>one Claude credential</strong>, preserving the source:</p>\n<pre><code class=\"language-sh\">xcb accounts import-agentmixer --source /absolute/path/to/.agentmixer --label imported\n</code></pre>\n<p>It does not migrate transcripts or sessions. The compatibility source has its\nown <code>migrate</code> command and identifier changes; see the\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/compatibility.md#migrating-from-agentmixer\">compatibility migration reference</a>.\nDo not run compatibility migration commands against the native state root.</p>\n<h2 id=\"standalone-package\">Standalone package</h2>\n<p>The retained TypeScript source provides host-owned routing, account custody,\nbounded tools, and provider adapters. It is separate from the native Rust app.\nFor building it locally, library examples, qualification requirements, and its\nCLI commands, see the <a href=\"https://github.com/hraness/xcb/blob/main/docs/compatibility.md\">compatibility reference</a> and\n<a href=\"https://github.com/hraness/xcb/blob/main/MANAGED-CODEX.md\">managed Codex contract</a>. The\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/publishing.md\">publishing contract</a> describes future verified artifacts;\nit is not evidence of a published package.</p>\n<h2 id=\"development\">Development</h2>\n<p>See <a href=\"https://github.com/hraness/xcb/blob/main/CONTRIBUTING.md\">Contributing</a> for setup and the native, compatibility, and\nsite checks. The credential-free <a href=\"https://github.com/hraness/xcb/blob/main/qualification/codex-native.md\">native Codex boundary fixtures</a>\nand <a href=\"https://github.com/hraness/xcb/blob/main/qualification/devin-native.md\">native Devin boundary fixture</a> document\nrepeatable checks separately from authenticated live acceptance.\nReport vulnerabilities through <a href=\"https://github.com/hraness/xcb/blob/main/SECURITY.md\">Security</a>. Licensed under <a href=\"https://github.com/hraness/xcb/blob/main/LICENSE\">MIT</a>.</p>\n";
