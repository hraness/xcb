// Generated from ../README.md by scripts/sync-readme.ts. Do not edit.
export const readmeTitle = "xcb";
export const readmeLead = "xcb, short for Excalibur, routes coding tasks across the Claude, Codex, and Devin subscriptions you already pay for. Each task runs on an account that is signed in, idle, and not at a known usage limit, on a model that fits the work, and xcb holds that account until the provider process has exited.";
export const readmeHtml = "<h1 id=\"xcb\">xcb</h1>\n<p>xcb, short for Excalibur, routes coding tasks across the Claude, Codex, and\nDevin subscriptions you already pay for. Each task runs on an account that is\nsigned in, idle, and not at a known usage limit, on a model that fits the\nwork, and xcb holds that account until the provider process has exited.</p>\n<p>It is for developers who pay for more than one coding agent and want one\nworkflow around them: another agent can hand it work with one JSON command,\n<code>xcb --json route</code>, an application can embed the TypeScript SDK, and the\nterminal workspace is built on the same router. The native Rust app is a\nsource preview for supported Claude, Codex, and Devin runtimes. It includes\nworkspace file tools, an isolated Linux command runner, customizable panes,\nand a separate application API. The managed harness, which is being rebuilt as\na self-evolving ALGAL harness, is in development and does not run\nself-modifying routing policies. It does not replace every feature of the\nproviders' own tools; provider support and limits are listed below.</p>\n<p><a href=\"https://xcb.sh\">Project site</a> · <a href=\"https://xcb.sh/docs/getting-started\">Getting started</a> ·\n<a href=\"https://xcb.sh/compare\">Compare tools</a> · <a href=\"https://github.com/hraness/xcb\">Source</a> ·\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/route.md\">Route contract</a> · <a href=\"https://github.com/hraness/xcb/blob/main/docs/application-api.md\">Application API</a> · <a href=\"https://github.com/hraness/xcb/blob/main/docs/compatibility.md\">Compatibility reference</a> · <a href=\"https://github.com/hraness/xcb/blob/main/CONTRIBUTING.md\">Contributing</a></p>\n<p>xcb picks one signed-in, idle account for each task and keeps it locked until\nthe provider process has exited, so permission stays explicit: the design every\nHraness project shares.\n<a href=\"https://hraness.com/writing/the-thread-through-hraness\">The thread through hraness</a>\nfollows that design across the projects, and the\n<a href=\"https://algal.computer/docs/vision/\">ALGAL vision</a> states the bet behind it.</p>\n<h2 id=\"readiness\">Readiness</h2>\n<p><strong>xcb is not yet a daily-driver replacement for Codex, Claude Code, and Devin.</strong>\nThe native broker lists, reads, searches, and writes workspace files, creates\ndirectories, and removes or renames regular files with revision checks. The\nsource also includes an isolated Linux command runner for tests and builds on\nmacOS ARM64. The current backend passed its 12-case VM boundary suite, including\nfiltered Git inspection, public dependency fetching, and offline Cargo/Bun use\nfrom immutable caches. Installed Claude and Codex coding workflows passed on\nmacOS ARM64: an expected test failure, exact repair, passing test, and filtered\nGit status, with joined processes and settled effects. This evidence covers the\ntested accounts and admitted builds. Devin's credential-free boundary checks\nare separate from authenticated coding acceptance. See the <a href=\"https://github.com/hraness/xcb/blob/main/docs/command-runner.md\">command runner contract</a>\nfor setup, supported boundaries, and current limits.</p>\n<table>\n<thead>\n<tr><th>Provider</th><th>Native Rust CLI</th><th>TypeScript compatibility CLI</th></tr>\n</thead>\n<tbody>\n<tr><td>Claude</td><td>Installed coding workflow verified on macOS ARM64 with the tested account; Linux remains an execution candidate after sign-in, binary admission, and confinement checks</td><td>Execution candidate, subject to its own admission and confinement checks</td></tr>\n<tr><td>Codex</td><td>Native app-server on macOS for exact build <strong>0.156.1</strong>; credential-free boundary and tool-manifest checks passed; authenticated coding acceptance was recorded on the previous admitted build and has not been rerun on this one</td><td>Discovery only; managed task execution gated on host qualification</td></tr>\n<tr><td>Devin</td><td>Native ACP candidate on macOS for exact builds <strong>3000.11.3</strong>, <strong>3000.11.1</strong> and <strong>3000.10.31</strong>; all passed credential-free boundary checks; model availability is checked against the connected account's fresh catalog at launch</td><td>ACP implementation exists; task execution disabled pending exact-runtime qualification</td></tr>\n</tbody>\n</table>\n<p>A successful <code>doctor</code> or a visible model does not prove a working coding session.\nThe current Devin CLI can be authenticated and can return its model catalog, but\nthat provider login is separate from xcb's explicit credential import and from\nan admitted coding turn. Devin validates the selected model against the\nconnected account's fresh catalog before each turn. The September 20, 2026\nquota result is historical evidence, not a statement of current availability.\n<code>xcb doctor</code> marks an unqualified provider build with ⚠ (&quot;found, but xcb\ncan't run this build yet&quot;). Codex and\nDevin candidates require the checked executable digest as well as the version;\nother builds and their Linux execution paths remain unavailable. Automated\nfixtures check boundaries; they do not establish authentication, service\nreliability, or real-model task quality. The TypeScript compatibility CLI's\nCodex and Devin task routes remain unqualified and disabled.</p>\n<h2 id=\"native-xcb\">Native xcb</h2>\n<p>Native release binaries are built for macOS ARM64 (<code>darwin-aarch64</code>) and\nLinux x86_64 (<code>linux-x86_64</code>) as <code>xcb-&lt;version&gt;-&lt;platform&gt;.tar.gz</code> with an\nadjacent <code>.sha256</code> checksum; other hosts build from source. The\n<a href=\"https://github.com/hraness/xcb/releases\">release assets</a> show the latest\nverified version and the <a href=\"https://xcb.sh/download\">project site</a> reflects the\nsame datum. The source version number is a build identity, not a published\nrelease. Releases tagged v0.3.0 and earlier are AgentMixer package archives,\nnot native xcb binaries.</p>\n<h3 id=\"install-a-verified-release\">Install a verified release</h3>\n<p>Latest verified release: <strong>v0.8.13</strong> · <a href=\"https://github.com/hraness/xcb/actions/runs/36262248568\">public verification run</a> · <a href=\"https://github.com/hraness/xcb/releases/tag/v0.8.13\">release notes</a>.</p>\n<ul>\n<li>macOS ARM64 (Apple silicon): <a href=\"https://github.com/hraness/xcb/releases/download/v0.8.13/xcb-0.8.13-darwin-aarch64.tar.gz\">xcb-0.8.13-darwin-aarch64.tar.gz</a> · <a href=\"https://github.com/hraness/xcb/releases/download/v0.8.13/xcb-0.8.13-darwin-aarch64.tar.gz.sha256\">SHA-256</a></li>\n<li>Linux x86_64: <a href=\"https://github.com/hraness/xcb/releases/download/v0.8.13/xcb-0.8.13-linux-x86_64.tar.gz\">xcb-0.8.13-linux-x86_64.tar.gz</a> · <a href=\"https://github.com/hraness/xcb/releases/download/v0.8.13/xcb-0.8.13-linux-x86_64.tar.gz.sha256\">SHA-256</a></li>\n<li><a href=\"https://github.com/hraness/xcb/releases/download/v0.8.13/hraness-xcb-0.8.13.tgz\">TypeScript compatibility archive</a> (separate from the native app)</li>\n</ul>\n<p>On a supported platform, download the archive and checksum for your host from\nthe release assets, or let the installer fetch and verify one exact version\nfrom a source checkout:</p>\n<pre><code class=\"language-sh\">XCB_VERSION=&lt;version&gt; ./scripts/install-native.sh\n</code></pre>\n<p>The installer refuses a missing archive, a checksum mismatch, or an archive\nthat contains anything other than the <code>xcb</code> binary. When no verified native\nrelease exists yet, install from source instead.</p>\n<h3 id=\"install-from-source\">Install from source</h3>\n<p>Requires Git, the pinned Rust <strong>1.97.1</strong> toolchain, and platform build tools.\nClaude's supported execution boundary is macOS Seatbelt or Linux with a working,\nadmitted <code>bwrap</code> configuration. The native Codex and Devin candidates currently\nrequire macOS Seatbelt; unsupported confinement fails closed.</p>\n<pre><code class=\"language-sh\">git clone https://github.com/hraness/xcb.git\ncd xcb\nrustup toolchain install 1.97.1 --profile minimal\n./scripts/install-native.sh\nexport PATH=&quot;$HOME/.local/bin:$PATH&quot;\nxcb --version\nxcb --help\n</code></pre>\n<p>The installer builds with the lockfile and installs <code>~/.local/bin/xcb</code>.\n<code>XCB_INSTALL_PREFIX</code> changes the prefix; <code>XCB_ADD_PATH=yes</code> appends the bin\ndirectory to your shell profile when it is not already on <code>PATH</code>. The\nTypeScript compatibility CLI installs as <code>xcb-compat</code>, so it does not shadow\nthe native <code>xcb</code>; an older compatibility install that still used the <code>xcb</code>\nname should be removed, and <code>command -v xcb</code> shows which binary answers.\nThe installer also records a private install manifest under the prefix and\nkeeps the exact installer beside the binary, so later upgrades use the same\nverified path.</p>\n<h3 id=\"updates-and-global-operation\">Updates and global operation</h3>\n<p>The native binary is a user-global install when it lives in <code>~/.local/bin</code> and\nthat directory is on <code>PATH</code>. xcb follows an OpenCode-style policy: <code>notify</code> is\nthe default, <code>auto</code> installs only an exact stable release with its checksum,\nand <code>disable</code> turns checks off. Scheduled checks are macOS-only: a LaunchAgent\nruns the check once a day when you enable it, and it never reads project\nsettings or updates from <code>main</code>. On Linux, run <code>xcb update check</code> from your\nown user timer; <code>xcb update enable</code> reports that scheduling is unavailable.</p>\n<pre><code class=\"language-sh\">xcb update check\nxcb update enable --policy notify   # macOS only: check daily and tell you when a release exists\nxcb update enable --policy auto     # macOS only: check daily and install verified releases\nxcb update status\nxcb upgrade                         # install the latest verified native release\nxcb update disable\n</code></pre>\n<p>Native release binaries are built for macOS ARM64 (<code>darwin-aarch64</code>) and\nLinux x86_64 (<code>linux-x86_64</code>). The updater installs only a verified\n<code>xcb-&lt;version&gt;-&lt;platform&gt;.tar.gz</code> asset with its matching checksum for the\nrunning host; on any other host, or when no such release exists yet, it fails\nclosed and leaves the installed binary alone. After any replacement, restart\nopen terminals; provider pins rebind to the new host build automatically while\ntheir pinned provider bytes are unchanged.</p>\n<p>Managed supervisors record their exact executable identity. When that binary\nis replaced, a current supervisor stops starting new turns, retains custody of\nits active workers until they settle, then exits. Queued tasks and tasks waiting\nfor input remain saved. Wait for that exit, restart the terminal, and reopen\nthe control conversation to continue; provider pins rebind automatically.\nA different running build produces an explicit supervisor-version error.\nLegacy supervisors without an identity record need their exact process verified\nand stopped after active workers settle; a saved PID or a deleted lock file is\nnot a safe replacement for that verification.</p>\n<p>Managed records can gain fields that older source builds reject. Restart old\nclients and supervisors before using updated managed state, and retain that state\nduring an installation rollback. Replacing the binary does not migrate provider\nsessions or establish fresh live acceptance across all three providers.</p>\n<h3 id=\"first-managed-conversation\">First managed conversation</h3>\n<p>Install an admitted Claude Code binary (major 2, version 2.1.268 or newer).\nxcb performs its own account sign-in below; it does\nnot silently import your existing provider login. Replace <code>&lt;account-id&gt;</code> below\nwith the generated ID printed by <code>accounts add</code> or <code>accounts import-*</code> (the\n<code>xcb accounts</code> ID column is shortened; <code>xcb accounts --json</code> lists full IDs).\nAccount names come from observed provider identities;\ncustom labels are not accepted.</p>\n<pre><code class=\"language-sh\">xcb setup claude --plan Max\nxcb --cwd /absolute/path/to/your/project\n</code></pre>\n<p><code>xcb setup</code> adds the account (or reuses the one you have), checks Claude Code\nthe way <code>xcb doctor</code> does, opens the browser sign-in, and loads the account's\nmodels. Each step is also its own command:</p>\n<pre><code class=\"language-sh\">xcb accounts add claude --plan Max\nxcb doctor --provider claude\nxcb accounts login &lt;account-id&gt;\nxcb accounts refresh &lt;account-id&gt;\nxcb models\n</code></pre>\n<p>Plain <code>xcb</code> reopens the latest persistent control conversation for the workspace;\n<code>xcb chat --new</code> starts another. Prompts become durable\nmanaged tasks routed through admitted Codex, Claude, or Devin sessions; closing\nthe terminal detaches without cancelling them. Open another terminal for an\nindependent conversation over the same task swarm, use <code>/tasks</code> to inspect work,\nor <code>/resume</code> to switch control conversations. Ordinary prompts create new work.\nIn the v0.7 terminal, select a task in <code>/agents</code> and press <code>s</code> to guide it or <code>a</code>\nto answer its current question; the prompt displays the target. <code>/task</code> returns\nto new work and Tab queues it. Independent workspaces can run concurrently;\ntasks in the same workspace run one at a time. Use\n<code>/cancel &lt;task-id&gt;</code> to request cancellation and inspect <code>/tasks</code> for settlement.</p>\n<p>Use <code>/backlog</code> for this conversation's backlog and <code>/backlog all</code> to browse all\nprojects. <code>/attention</code> collects questions, approvals and actions across agents.\nUse <code>/steer &lt;task-id&gt; &lt;guidance&gt;</code> to queue guidance for a task's next safe turn,\nand <code>/inbox</code> to inspect acceptance and delivery. <code>/watch &lt;target-id&gt; &lt;source-id&gt;</code>\nrequests a completion report in the target's inbox. Available reports and messages\nshare a bounded batch; they do not renew budgets or reopen closed work. The CLI\noffers the same <code>steer</code>, <code>watch</code> and <code>inbox</code> controls, including stable IDs for\nretries and paginated JSON inspection.\nDeferred work can be edited, released, or completed with a summary. <code>/project grant &lt;tasks&gt; &lt;hours&gt; &lt;goal&gt;</code> delegates a bounded follow-up budget; <code>/project pause</code> holds future automatic work. <code>/schedule</code> manages recurring prompts, and\n<code>xcb schedules program</code> pins bounded ALGAL planners. Starting in v0.6.0, add\n<code>--managed-calls 2</code>\nto run a controller that can suspend for up to two ordinary worker tasks, or use\n<code>xcb backlog program</code> to run one immediately. <code>/program</code> and\n<code>xcb backlog program-status &lt;task-id&gt;</code> show its linked child, progress and\nreceipt. Every occurrence retains its normal task history and\nattention states. Workers can propose follow-ups,\nread recent summaries, and search an explicitly bound Wordcell vault. Explicit\nnote promotion keeps long-term knowledge separate from working memory. See\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/project-agents.md\">persistent project agents</a> and <a href=\"https://github.com/hraness/xcb/blob/main/docs/habitat-service.md\">opt-in login\nstartup</a> for controls and limits.</p>\n<p>The Rust supervisor owns scheduling and deterministic safety decisions. ALGAL\nrecords bounded transition receipts; it does not infer permissions, establish\nprovider qualification, or replace the supervisor’s execution policy.\n<code>xcb tasks verify &lt;task-id&gt;</code> replays that task’s local receipt chain and checks\nit against the current record; it does not attest provider claims or real-world\noutcomes.</p>\n<p>Managed routing and unpinned <code>xcb run</code> first filter for qualified, credentialed,\nidle, quota-usable accounts. ALGAL's fitted classifier can select a capability\ntier through one bounded typed judgment; routing works deterministically when\nthat optional service is absent. Substantial prompts use the highest known\nquality among eligible models. Quota-driven downgrades are visible. Explicit\nprovider/model requests remain constraints; routine work still considers\nrelative cost, latency and workspace preferences. Official temporary\noffers are cached as expiring observations. They do not prove account entitlement\nor reduce a route’s estimated cost without that evidence. They never activate an\nunqualified provider or survive stale terms. Managed Claude, Codex and Devin workers share\n<code>xcb_swarm_status</code>, <code>xcb_message_list</code> and <code>xcb_message_send</code> for durable,\nworkspace-scoped cross-provider coordination.</p>\n<p>A settled authentication failure marks that account as requiring reconnection\nand excludes it from new task routes, including after restart. Other eligible\naccounts still respect the requested provider. Successful sign-in or an explicit\nimport with changed credential material clears the block; catalog refresh and\nreimporting the same credentials do not. Older failure records have no credential\ngeneration binding, so an upgraded account may need one new bounded attempt to\nestablish this block.</p>\n<p><code>--plan</code> is a display label; it does not verify your subscription. Complete the\nbrowser sign-in when prompted. <code>accounts refresh</code> probes supported model and\nusage metadata. Unknown or stale usage percentages remain unknown. A proven\nClaude account-wide quota exhaustion stays blocked until its reported reset,\neven when its percentage has gone stale. The account list shows a retry estimate;\nsee <a href=\"https://github.com/hraness/xcb/blob/main/docs/quota-routing.md\">quota routing</a> for the scope and credential binding.\nYou do not need to select a model for managed chat or <code>xcb run</code>. To pin a model\nfor a direct run, pass its full observed key with <code>--model</code>; stored direct\nsessions keep their existing binding. Begin a managed task with <code>Use Claude</code>,\n<code>Use Codex</code>, or <code>Use Devin</code> when you want to require that provider.</p>\n<p>If discovery finds the wrong binary, use\n<code>xcb doctor --provider claude --executable /absolute/path/to/claude</code>.\nThe pin binds a private copy of the executable bytes plus version, so a\nprovider auto-update cannot move the pinned install: xcb adopts an updated\nbuild automatically only when it is an admitted version and keeps routing the\npinned build otherwise. For exact-artifact providers, admission also accepts\nreviewed (version, digest) pairs published in the repository's\n<code>qualified-builds.json</code>: a discovered build nothing yet admits is parked as\nawaiting catalog admission (<code>xcb doctor</code> reports it), and a published entry\nadopts it on the next hourly refresh without an xcb upgrade. The catalog is\nadmission-only — a denied digest is rejected outright, and the stored copy\nis reused when the network is unavailable. After upgrading xcb, restart open\nxcb terminals; the\npins rebind to the new host build on next use. A process started from the old\nbinary cannot adopt the replacement binary's pin, and older clients refuse\nnew run records whose credential-custody format they do not understand.\nAn account, metadata pin, or model listing cannot activate an unqualified adapter.</p>\n<h3 id=\"connect-codex-on-macos\">Connect Codex on macOS</h3>\n<p>Use the exact admitted <strong>0.156.1</strong> build. Its credential-free boundary and\ntool-manifest checks passed on macOS ARM64. Authenticated broker read/write/read\nand installed coding-workflow acceptance were recorded on the previous admitted\nbuild (0.155.0-alpha.2.6) and have not been rerun on this one. None of this\nqualifies arbitrary provider versions or the separate application API. xcb supervises the official\nCLI's ChatGPT device sign-in in a private profile:</p>\n<pre><code class=\"language-sh\">xcb doctor --provider codex\nxcb accounts add codex --plan ChatGPT\nxcb accounts login &lt;account-id&gt;\nxcb accounts refresh &lt;account-id&gt;\nxcb models\n</code></pre>\n<p>Follow the device sign-in instructions shown in the terminal. Alternatively,\ncopy one existing ChatGPT credential into a new xcb account by selecting its\nprivate <code>auth.json</code> explicitly:</p>\n<pre><code class=\"language-sh\">xcb accounts import-codex --source /absolute/path/to/auth.json\nxcb accounts refresh &lt;account-id&gt;\n</code></pre>\n<p>The source file is preserved. Import does not copy provider configuration,\nplugins, sessions, or transcripts. API-key credentials are not accepted by this\nroute. Refreshed ChatGPT credentials are persisted only after the owned provider\nprocess has joined.</p>\n<h3 id=\"connect-devin-on-macos\">Connect Devin on macOS</h3>\n<p>Use the exact admitted <strong>3000.11.3</strong> build; <strong>3000.11.1</strong> and <strong>3000.10.31</strong>\nremain admitted. All passed credential-free native boundary checks; authenticated coding\nacceptance requires separate evidence for the account, model, and build.\nModel availability is checked against the connected account's fresh catalog at\nlaunch. xcb preserves an unknown quota reset as unknown. Sign in through the\nprovider CLI, then explicitly select its\n<code>credentials.toml</code> to create a private xcb account:</p>\n<pre><code class=\"language-sh\">devin auth login\nxcb doctor --provider devin\nxcb accounts import-devin --source /absolute/path/to/credentials.toml\nxcb accounts refresh &lt;account-id&gt;\nxcb models\n</code></pre>\n<p>The source file and provider sessions are preserved. xcb copies only the\ncredential for the supported provider endpoints. To update just the catalog,\nuse <code>xcb models refresh devin --account &lt;account-id&gt;</code>; Devin discovery requires\nan explicitly connected account. Native Devin currently uses fixed ACP model\nchoices. Adaptive and Fusion catalog representations in the compatibility\npackage do not establish native support.</p>\n<p>For either provider, <code>xcb run</code> automatically selects an eligible model. An\noptional explicit default for direct interactive sessions can be set with a\nfull matching key from <code>xcb models</code> using <code>xcb models default &lt;key&gt;</code>. Select the account with\n<code>xcb accounts default &lt;account&gt;</code> for new direct sessions, or pass\n<code>--account &lt;account&gt; --model &lt;key&gt;</code> to <code>xcb run</code>. A provider upgrade is not\nautomatically admitted; xcb keeps routing the pinned qualified build until an\nxcb release qualifies the new artifact.</p>\n<h3 id=\"isolated-tests-builds-and-git\">Isolated tests, builds, and Git</h3>\n<p>Project commands use an explicitly provisioned Linux VM through <code>workspace_exec</code>.\nFollow the <a href=\"https://github.com/hraness/xcb/blob/main/docs/command-runner.md#setup-and-admission\">command runner setup</a>\nfrom the same source checkout as the installed native CLI. Commands run offline\nagainst a staged workspace; host dependencies, credentials, and build products\nare excluded. Native macOS and Xcode builds are unavailable. The explicit\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/command-runner.md#dependencies-and-git\">public dependency preparation frontend</a>\npassed the current VM boundary suite, including rejection of a cache after its\nmanifest changed. Installed Claude and Codex coding workflows passed on macOS\nARM64 with the tested accounts; other repositories and toolchains still need\ntheir own checks.</p>\n<p>The Git projection is limited to filtered, read-only HEAD and index data for\nstatus and diffs. Original history, remotes, and hooks are omitted; commit and\npush workflows are unavailable. Publication checks file revisions and replaces\neach file atomically; it is not a transaction across every changed file. Failed,\ncancelled, or uncertain command state is retained. Successfully published and\ndurably settled commands remove their verified input snapshot.</p>\n<h3 id=\"application-integration\">Application integration</h3>\n<p>The <a href=\"https://github.com/hraness/xcb/blob/main/docs/application-api.md\">application API</a> provides bounded, ephemeral\ninference with no tools or hooks. It requires evidence for the exact xcb binary,\nprovider, account and model before accepting application traffic.\n<a href=\"https://github.com/hraness/textbutler\">Textbutler</a>, an MIT-licensed reference\napplication, keeps its contact access and messaging approval in its own host.\nSign-in and a successful <code>doctor</code> alone do not qualify the application route.</p>\n<h3 id=\"everyday-commands\">Everyday commands</h3>\n<pre><code class=\"language-sh\">xcb --cwd /absolute/path/to/your/project       # new control conversation\nxcb conversations                                # resumable control conversations\nxcb chat --resume &lt;conversation-id&gt;\nxcb tasks                                        # global managed task swarm\nxcb backlog                                      # backlog and work history across projects\nxcb attention                                    # questions, approvals and actions\nxcb schedules                                    # durable recurring prompts\nxcb projects                                     # project goals and remaining autonomy grants\nxcb memory status &lt;conversation-id&gt;               # explicit Wordcell binding\nxcb service status                               # opt-in macOS login startup\nxcb tasks verify &lt;task-id&gt;                       # verify local transition receipts\nxcb tasks messages &lt;task-id&gt;                     # durable cross-provider mailbox\nxcb offers --refresh                             # refresh official expiring offers\nxcb models tiers --task &quot;fix a race&quot;             # inspect Pareto layers\nxcb models route --task &quot;fix a race&quot;             # preview the eligible smart route\nxcb --cwd /absolute/path/to/your/project run --account &lt;account-id&gt; -p &quot;Explain this repository&quot;\nxcb sessions                                     # direct provider sessions\nxcb resume                                       # latest direct provider session\nxcb resume &lt;session-id&gt;\nxcb accounts\nxcb config\nxcb plugins\nxcb panes\nxcb doctor\nxcb completions zsh &gt; /path/to/completions/_xcb\n</code></pre>\n<p>For another program — typically a coding agent — <code>xcb --json route</code> is the\nclosed machine contract: one JSON task document on stdin selects an eligible\naccount/model route and runs exactly one bounded turn, returning the selected\nroute, saved session id, and settled outcome facts as bounded JSON. See\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/route.md\">the route contract</a>.</p>\n<p><code>xcb chat --resume</code> reopens a control conversation; <code>xcb resume</code> opens a saved\ndirect provider session and its workspace. Neither is a headless continuation\ncommand. <code>/help</code> lists terminal commands. The <a href=\"https://github.com/hraness/xcb/blob/main/docs/terminal.md\">terminal guide</a>\ncovers editing keys, optional Vim editing, transcript search, agent guidance,\nand draft recovery.\nConversations, tasks, provider\nsessions, and credentials live in the private native state root\n<code>~/.local/share/xcb</code>; <code>--state /absolute/path</code> or <code>XCB_STATE</code> overrides it.\nThe <code>xcb-compat</code> compatibility CLI uses <code>~/.xcb</code> instead. Do not point both\nimplementations at the same state directory. <code>xcb --json run</code> includes the native session ID\nin its result so it can be reopened with <code>xcb resume &lt;session-id&gt;</code>.</p>\n<p>One provider turn has a 30-minute default deadline, including initialization.\nThe <code>turn_timeout_ms</code> setting in the private state root's <code>config.json</code> accepts\n1,000–3,600,000 milliseconds (one second to 60 minutes); <code>xcb config</code> displays the\neffective configuration. Older configurations that omit it use the default.\nCancellation remains available before the deadline, and automatic continuation\nhas its own separate limits.</p>\n<p>Control conversations are concurrent and share one durable task supervisor.\nEach account still owns at most one active provider turn, and one workspace can\nhave only one admitted writer even when different accounts or terminals are\navailable. Independent workspaces and accounts can run concurrently. Managed\ncancellation may be requested from the task’s originating conversation or by an\nexplicit task ID/title elsewhere; direct provider-session cancellation remains\nowned by its terminal. Ctrl-C and SIGTERM request bounded cleanup for a headless\nrun. <code>xcb run</code> reports success only for a completed, joined, settled idle result.</p>\n<p>Cancellation joins the owned process before releasing custody. If <code>doctor</code>\nreports an unsettled run, inspect <code>xcb recover</code> and the process state; an expired\nlease or a quiet terminal is not proof that the provider stopped. Recovery is\nan explicit operation, not a reason to delete state or lock files.</p>\n<h3 id=\"fleet-and-remote-devices\">Fleet and remote devices</h3>\n<p>Each machine can enroll as a device on a shared relay fleet. Linked machines\npublish an encrypted presence and task projection and accept fenced commands;\na controller device or another enrolled machine drives them through the same\nCLI. Remote task content stays encrypted at the device boundary.</p>\n<pre><code class=\"language-sh\">xcb link --relay &lt;relay-url&gt; --email &lt;owner email&gt;   # enroll this machine (email one-time code)\nxcb remote admit &lt;device&gt;                          # admit a newly linked device\nxcb fleet                                          # devices, presence, projection staleness\nxcb dispatch &lt;device&gt; &lt;workspace&gt; -p &quot;&lt;task&gt;&quot;      # run a managed task on a remote machine\nxcb remote status &lt;command-id&gt; --wait              # wait for a posted command to settle\nxcb remote steer|cancel|answer &lt;device&gt; &lt;task&gt;     # drive a remote task\n</code></pre>\n<p>Ids may be typed as unambiguous prefixes (<code>xcb remote cancel 513c t_a65b</code>).\nEvery remote verb is <code>--json</code>-scriptable with closed exit codes, which is the\ncontract a driving agent (for example a bot) should use. See\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/remote-operations.md\">remote operations</a> for enrollment, controller\nsemantics and recovery.</p>\n<h3 id=\"optional-behavior\">Optional behavior</h3>\n<p>Auto-continue and Gobstopper context management default on with bounded\ncontinuation and settled-boundary checks. Disable either with\n<code>xcb plugins disable auto-continue</code> or <code>xcb plugins disable gobstopper</code>.\nLocal usage measurement stays local. aiCharts upload is unavailable; local\nexports, external judgment, and executable hooks require separate opt-in.\nPanes are presentation data and cannot grant execution authority.</p>\n<p>The optional judge uses TypeSafe System One (<code>jev-latest</code>) to advise routing,\ncontinuation, and context retention. It sends bounded task/response context to\nthat service; old tool-result bodies are excluded from compaction advice.\nA judge cannot qualify a provider or bypass deterministic safety gates.\nStore a key through stdin on macOS or Linux, then explicitly enable it:</p>\n<pre><code class=\"language-sh\">xcb judge token &lt; /secure/path/to/judge-key\nxcb judge status\nxcb judge enable\nxcb judge test\n# Later:\nxcb judge disable\nxcb judge logout\n</code></pre>\n<p>The input file is an existing private credential file, not a command-line key.\nKeys are vaulted mode-0600 outside the workspace. <code>XCB_JEV_API_KEY</code> or\n<code>TYPESAFE_API_KEY</code> may supply a key via the environment. Custom endpoints require\nan explicit environment key; the vaulted key remains bound to System One.</p>\n<h2 id=\"migrating-from-agentmixer\">Migrating from AgentMixer</h2>\n<p>The native command imports <strong>one Claude credential</strong>, preserving the source:</p>\n<pre><code class=\"language-sh\">xcb accounts import-agentmixer --source /absolute/path/to/.agentmixer\n</code></pre>\n<p>The account name comes from the observed provider identity. It does not\nmigrate transcripts or sessions. The <code>xcb-compat</code> compatibility CLI has its\nown <code>migrate</code> command and identifier changes; see the\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/compatibility.md#migrating-from-agentmixer\">compatibility migration reference</a>.\nDo not run compatibility migration commands against the native state root.</p>\n<h2 id=\"standalone-package\">Standalone package</h2>\n<p>The retained TypeScript source provides host-owned routing, account custody,\nbounded tools, and provider adapters. It is separate from the native Rust app.\nFor building it locally, library examples, qualification requirements, and its\nCLI commands, see the <a href=\"https://github.com/hraness/xcb/blob/main/docs/compatibility.md\">compatibility reference</a> and\n<a href=\"https://github.com/hraness/xcb/blob/main/MANAGED-CODEX.md\">managed Codex contract</a>. The\n<a href=\"https://github.com/hraness/xcb/blob/main/docs/publishing.md\">publishing contract</a> describes future verified artifacts;\nit is not evidence of a published package.</p>\n<h2 id=\"development\">Development</h2>\n<p>See <a href=\"https://github.com/hraness/xcb/blob/main/CONTRIBUTING.md\">Contributing</a> for setup and the native, compatibility, and\nsite checks. The credential-free <a href=\"https://github.com/hraness/xcb/blob/main/qualification/codex-native.md\">native Codex boundary fixtures</a>\nand <a href=\"https://github.com/hraness/xcb/blob/main/qualification/devin-native.md\">native Devin boundary fixture</a> document\nrepeatable checks separately from authenticated live acceptance.\nRelease notes live in <a href=\"https://github.com/hraness/xcb/blob/main/CHANGELOG.md\">CHANGELOG.md</a>.\nReport vulnerabilities through <a href=\"https://github.com/hraness/xcb/blob/main/SECURITY.md\">Security</a>. Licensed under <a href=\"https://github.com/hraness/xcb/blob/main/LICENSE\">MIT</a>.</p>\n";
export const readmeMarkdown = "# xcb\n\nxcb, short for Excalibur, routes coding tasks across the Claude, Codex, and\nDevin subscriptions you already pay for. Each task runs on an account that is\nsigned in, idle, and not at a known usage limit, on a model that fits the\nwork, and xcb holds that account until the provider process has exited.\n\nIt is for developers who pay for more than one coding agent and want one\nworkflow around them: another agent can hand it work with one JSON command,\n`xcb --json route`, an application can embed the TypeScript SDK, and the\nterminal workspace is built on the same router. The native Rust app is a\nsource preview for supported Claude, Codex, and Devin runtimes. It includes\nworkspace file tools, an isolated Linux command runner, customizable panes,\nand a separate application API. The managed harness, which is being rebuilt as\na self-evolving ALGAL harness, is in development and does not run\nself-modifying routing policies. It does not replace every feature of the\nproviders' own tools; provider support and limits are listed below.\n\n\n[Project site](https://xcb.sh) · [Getting started](https://xcb.sh/docs/getting-started) ·\n[Compare tools](https://xcb.sh/compare) · [Source](https://github.com/hraness/xcb) ·\n[Route contract](https://github.com/hraness/xcb/blob/main/docs/route.md) · [Application API](https://github.com/hraness/xcb/blob/main/docs/application-api.md) · [Compatibility reference](https://github.com/hraness/xcb/blob/main/docs/compatibility.md) · [Contributing](https://github.com/hraness/xcb/blob/main/CONTRIBUTING.md)\n\nxcb picks one signed-in, idle account for each task and keeps it locked until\nthe provider process has exited, so permission stays explicit: the design every\nHraness project shares.\n[The thread through hraness](https://hraness.com/writing/the-thread-through-hraness)\nfollows that design across the projects, and the\n[ALGAL vision](https://algal.computer/docs/vision/) states the bet behind it.\n\n## Readiness\n\n**xcb is not yet a daily-driver replacement for Codex, Claude Code, and Devin.**\nThe native broker lists, reads, searches, and writes workspace files, creates\ndirectories, and removes or renames regular files with revision checks. The\nsource also includes an isolated Linux command runner for tests and builds on\nmacOS ARM64. The current backend passed its 12-case VM boundary suite, including\nfiltered Git inspection, public dependency fetching, and offline Cargo/Bun use\nfrom immutable caches. Installed Claude and Codex coding workflows passed on\nmacOS ARM64: an expected test failure, exact repair, passing test, and filtered\nGit status, with joined processes and settled effects. This evidence covers the\ntested accounts and admitted builds. Devin's credential-free boundary checks\nare separate from authenticated coding acceptance. See the [command runner contract](https://github.com/hraness/xcb/blob/main/docs/command-runner.md)\nfor setup, supported boundaries, and current limits.\n\n| Provider | Native Rust CLI | TypeScript compatibility CLI |\n| --- | --- | --- |\n| Claude | Installed coding workflow verified on macOS ARM64 with the tested account; Linux remains an execution candidate after sign-in, binary admission, and confinement checks | Execution candidate, subject to its own admission and confinement checks |\n| Codex | Native app-server on macOS for exact build **0.156.1**; credential-free boundary and tool-manifest checks passed; authenticated coding acceptance was recorded on the previous admitted build and has not been rerun on this one | Discovery only; managed task execution gated on host qualification |\n| Devin | Native ACP candidate on macOS for exact builds **3000.11.3**, **3000.11.1** and **3000.10.31**; all passed credential-free boundary checks; model availability is checked against the connected account's fresh catalog at launch | ACP implementation exists; task execution disabled pending exact-runtime qualification |\n\nA successful `doctor` or a visible model does not prove a working coding session.\nThe current Devin CLI can be authenticated and can return its model catalog, but\nthat provider login is separate from xcb's explicit credential import and from\nan admitted coding turn. Devin validates the selected model against the\nconnected account's fresh catalog before each turn. The September 20, 2026\nquota result is historical evidence, not a statement of current availability.\n`xcb doctor` marks an unqualified provider build with ⚠ (\"found, but xcb\ncan't run this build yet\"). Codex and\nDevin candidates require the checked executable digest as well as the version;\nother builds and their Linux execution paths remain unavailable. Automated\nfixtures check boundaries; they do not establish authentication, service\nreliability, or real-model task quality. The TypeScript compatibility CLI's\nCodex and Devin task routes remain unqualified and disabled.\n\n## Native xcb\n\nNative release binaries are built for macOS ARM64 (`darwin-aarch64`) and\nLinux x86_64 (`linux-x86_64`) as `xcb-<version>-<platform>.tar.gz` with an\nadjacent `.sha256` checksum; other hosts build from source. The\n[release assets](https://github.com/hraness/xcb/releases) show the latest\nverified version and the [project site](https://xcb.sh/download) reflects the\nsame datum. The source version number is a build identity, not a published\nrelease. Releases tagged v0.3.0 and earlier are AgentMixer package archives,\nnot native xcb binaries.\n\n### Install a verified release\n\nLatest verified release: **v0.8.13** · [public verification run](https://github.com/hraness/xcb/actions/runs/36262248568) · [release notes](https://github.com/hraness/xcb/releases/tag/v0.8.13).\n\n- macOS ARM64 (Apple silicon): [xcb-0.8.13-darwin-aarch64.tar.gz](https://github.com/hraness/xcb/releases/download/v0.8.13/xcb-0.8.13-darwin-aarch64.tar.gz) · [SHA-256](https://github.com/hraness/xcb/releases/download/v0.8.13/xcb-0.8.13-darwin-aarch64.tar.gz.sha256)\n- Linux x86_64: [xcb-0.8.13-linux-x86_64.tar.gz](https://github.com/hraness/xcb/releases/download/v0.8.13/xcb-0.8.13-linux-x86_64.tar.gz) · [SHA-256](https://github.com/hraness/xcb/releases/download/v0.8.13/xcb-0.8.13-linux-x86_64.tar.gz.sha256)\n- [TypeScript compatibility archive](https://github.com/hraness/xcb/releases/download/v0.8.13/hraness-xcb-0.8.13.tgz) (separate from the native app)\n\nOn a supported platform, download the archive and checksum for your host from\nthe release assets, or let the installer fetch and verify one exact version\nfrom a source checkout:\n\n```sh\nXCB_VERSION=<version> ./scripts/install-native.sh\n```\n\nThe installer refuses a missing archive, a checksum mismatch, or an archive\nthat contains anything other than the `xcb` binary. When no verified native\nrelease exists yet, install from source instead.\n\n### Install from source\n\nRequires Git, the pinned Rust **1.97.1** toolchain, and platform build tools.\nClaude's supported execution boundary is macOS Seatbelt or Linux with a working,\nadmitted `bwrap` configuration. The native Codex and Devin candidates currently\nrequire macOS Seatbelt; unsupported confinement fails closed.\n\n```sh\ngit clone https://github.com/hraness/xcb.git\ncd xcb\nrustup toolchain install 1.97.1 --profile minimal\n./scripts/install-native.sh\nexport PATH=\"$HOME/.local/bin:$PATH\"\nxcb --version\nxcb --help\n```\n\nThe installer builds with the lockfile and installs `~/.local/bin/xcb`.\n`XCB_INSTALL_PREFIX` changes the prefix; `XCB_ADD_PATH=yes` appends the bin\ndirectory to your shell profile when it is not already on `PATH`. The\nTypeScript compatibility CLI installs as `xcb-compat`, so it does not shadow\nthe native `xcb`; an older compatibility install that still used the `xcb`\nname should be removed, and `command -v xcb` shows which binary answers.\nThe installer also records a private install manifest under the prefix and\nkeeps the exact installer beside the binary, so later upgrades use the same\nverified path.\n\n### Updates and global operation\n\nThe native binary is a user-global install when it lives in `~/.local/bin` and\nthat directory is on `PATH`. xcb follows an OpenCode-style policy: `notify` is\nthe default, `auto` installs only an exact stable release with its checksum,\nand `disable` turns checks off. Scheduled checks are macOS-only: a LaunchAgent\nruns the check once a day when you enable it, and it never reads project\nsettings or updates from `main`. On Linux, run `xcb update check` from your\nown user timer; `xcb update enable` reports that scheduling is unavailable.\n\n```sh\nxcb update check\nxcb update enable --policy notify   # macOS only: check daily and tell you when a release exists\nxcb update enable --policy auto     # macOS only: check daily and install verified releases\nxcb update status\nxcb upgrade                         # install the latest verified native release\nxcb update disable\n```\n\nNative release binaries are built for macOS ARM64 (`darwin-aarch64`) and\nLinux x86_64 (`linux-x86_64`). The updater installs only a verified\n`xcb-<version>-<platform>.tar.gz` asset with its matching checksum for the\nrunning host; on any other host, or when no such release exists yet, it fails\nclosed and leaves the installed binary alone. After any replacement, restart\nopen terminals; provider pins rebind to the new host build automatically while\ntheir pinned provider bytes are unchanged.\n\nManaged supervisors record their exact executable identity. When that binary\nis replaced, a current supervisor stops starting new turns, retains custody of\nits active workers until they settle, then exits. Queued tasks and tasks waiting\nfor input remain saved. Wait for that exit, restart the terminal, and reopen\nthe control conversation to continue; provider pins rebind automatically.\nA different running build produces an explicit supervisor-version error.\nLegacy supervisors without an identity record need their exact process verified\nand stopped after active workers settle; a saved PID or a deleted lock file is\nnot a safe replacement for that verification.\n\nManaged records can gain fields that older source builds reject. Restart old\nclients and supervisors before using updated managed state, and retain that state\nduring an installation rollback. Replacing the binary does not migrate provider\nsessions or establish fresh live acceptance across all three providers.\n\n### First managed conversation\n\nInstall an admitted Claude Code binary (major 2, version 2.1.268 or newer).\nxcb performs its own account sign-in below; it does\nnot silently import your existing provider login. Replace `<account-id>` below\nwith the generated ID printed by `accounts add` or `accounts import-*` (the\n`xcb accounts` ID column is shortened; `xcb accounts --json` lists full IDs).\nAccount names come from observed provider identities;\ncustom labels are not accepted.\n\n```sh\nxcb setup claude --plan Max\nxcb --cwd /absolute/path/to/your/project\n```\n\n`xcb setup` adds the account (or reuses the one you have), checks Claude Code\nthe way `xcb doctor` does, opens the browser sign-in, and loads the account's\nmodels. Each step is also its own command:\n\n```sh\nxcb accounts add claude --plan Max\nxcb doctor --provider claude\nxcb accounts login <account-id>\nxcb accounts refresh <account-id>\nxcb models\n```\n\nPlain `xcb` reopens the latest persistent control conversation for the workspace;\n`xcb chat --new` starts another. Prompts become durable\nmanaged tasks routed through admitted Codex, Claude, or Devin sessions; closing\nthe terminal detaches without cancelling them. Open another terminal for an\nindependent conversation over the same task swarm, use `/tasks` to inspect work,\nor `/resume` to switch control conversations. Ordinary prompts create new work.\nIn the v0.7 terminal, select a task in `/agents` and press `s` to guide it or `a`\nto answer its current question; the prompt displays the target. `/task` returns\nto new work and Tab queues it. Independent workspaces can run concurrently;\ntasks in the same workspace run one at a time. Use\n`/cancel <task-id>` to request cancellation and inspect `/tasks` for settlement.\n\nUse `/backlog` for this conversation's backlog and `/backlog all` to browse all\nprojects. `/attention` collects questions, approvals and actions across agents.\nUse `/steer <task-id> <guidance>` to queue guidance for a task's next safe turn,\nand `/inbox` to inspect acceptance and delivery. `/watch <target-id> <source-id>`\nrequests a completion report in the target's inbox. Available reports and messages\nshare a bounded batch; they do not renew budgets or reopen closed work. The CLI\noffers the same `steer`, `watch` and `inbox` controls, including stable IDs for\nretries and paginated JSON inspection.\nDeferred work can be edited, released, or completed with a summary. `/project\ngrant <tasks> <hours> <goal>` delegates a bounded follow-up budget; `/project\npause` holds future automatic work. `/schedule` manages recurring prompts, and\n`xcb schedules program` pins bounded ALGAL planners. Starting in v0.6.0, add\n`--managed-calls 2`\nto run a controller that can suspend for up to two ordinary worker tasks, or use\n`xcb backlog program` to run one immediately. `/program` and\n`xcb backlog program-status <task-id>` show its linked child, progress and\nreceipt. Every occurrence retains its normal task history and\nattention states. Workers can propose follow-ups,\nread recent summaries, and search an explicitly bound Wordcell vault. Explicit\nnote promotion keeps long-term knowledge separate from working memory. See\n[persistent project agents](https://github.com/hraness/xcb/blob/main/docs/project-agents.md) and [opt-in login\nstartup](docs/habitat-service.md) for controls and limits.\n\nThe Rust supervisor owns scheduling and deterministic safety decisions. ALGAL\nrecords bounded transition receipts; it does not infer permissions, establish\nprovider qualification, or replace the supervisor’s execution policy.\n`xcb tasks verify <task-id>` replays that task’s local receipt chain and checks\nit against the current record; it does not attest provider claims or real-world\noutcomes.\n\nManaged routing and unpinned `xcb run` first filter for qualified, credentialed,\nidle, quota-usable accounts. ALGAL's fitted classifier can select a capability\ntier through one bounded typed judgment; routing works deterministically when\nthat optional service is absent. Substantial prompts use the highest known\nquality among eligible models. Quota-driven downgrades are visible. Explicit\nprovider/model requests remain constraints; routine work still considers\nrelative cost, latency and workspace preferences. Official temporary\noffers are cached as expiring observations. They do not prove account entitlement\nor reduce a route’s estimated cost without that evidence. They never activate an\nunqualified provider or survive stale terms. Managed Claude, Codex and Devin workers share\n`xcb_swarm_status`, `xcb_message_list` and `xcb_message_send` for durable,\nworkspace-scoped cross-provider coordination.\n\nA settled authentication failure marks that account as requiring reconnection\nand excludes it from new task routes, including after restart. Other eligible\naccounts still respect the requested provider. Successful sign-in or an explicit\nimport with changed credential material clears the block; catalog refresh and\nreimporting the same credentials do not. Older failure records have no credential\ngeneration binding, so an upgraded account may need one new bounded attempt to\nestablish this block.\n\n`--plan` is a display label; it does not verify your subscription. Complete the\nbrowser sign-in when prompted. `accounts refresh` probes supported model and\nusage metadata. Unknown or stale usage percentages remain unknown. A proven\nClaude account-wide quota exhaustion stays blocked until its reported reset,\neven when its percentage has gone stale. The account list shows a retry estimate;\nsee [quota routing](https://github.com/hraness/xcb/blob/main/docs/quota-routing.md) for the scope and credential binding.\nYou do not need to select a model for managed chat or `xcb run`. To pin a model\nfor a direct run, pass its full observed key with `--model`; stored direct\nsessions keep their existing binding. Begin a managed task with `Use Claude`,\n`Use Codex`, or `Use Devin` when you want to require that provider.\n\nIf discovery finds the wrong binary, use\n`xcb doctor --provider claude --executable /absolute/path/to/claude`.\nThe pin binds a private copy of the executable bytes plus version, so a\nprovider auto-update cannot move the pinned install: xcb adopts an updated\nbuild automatically only when it is an admitted version and keeps routing the\npinned build otherwise. For exact-artifact providers, admission also accepts\nreviewed (version, digest) pairs published in the repository's\n`qualified-builds.json`: a discovered build nothing yet admits is parked as\nawaiting catalog admission (`xcb doctor` reports it), and a published entry\nadopts it on the next hourly refresh without an xcb upgrade. The catalog is\nadmission-only — a denied digest is rejected outright, and the stored copy\nis reused when the network is unavailable. After upgrading xcb, restart open\nxcb terminals; the\npins rebind to the new host build on next use. A process started from the old\nbinary cannot adopt the replacement binary's pin, and older clients refuse\nnew run records whose credential-custody format they do not understand.\nAn account, metadata pin, or model listing cannot activate an unqualified adapter.\n\n### Connect Codex on macOS\n\nUse the exact admitted **0.156.1** build. Its credential-free boundary and\ntool-manifest checks passed on macOS ARM64. Authenticated broker read/write/read\nand installed coding-workflow acceptance were recorded on the previous admitted\nbuild (0.155.0-alpha.2.6) and have not been rerun on this one. None of this\nqualifies arbitrary provider versions or the separate application API. xcb supervises the official\nCLI's ChatGPT device sign-in in a private profile:\n\n```sh\nxcb doctor --provider codex\nxcb accounts add codex --plan ChatGPT\nxcb accounts login <account-id>\nxcb accounts refresh <account-id>\nxcb models\n```\n\nFollow the device sign-in instructions shown in the terminal. Alternatively,\ncopy one existing ChatGPT credential into a new xcb account by selecting its\nprivate `auth.json` explicitly:\n\n```sh\nxcb accounts import-codex --source /absolute/path/to/auth.json\nxcb accounts refresh <account-id>\n```\n\nThe source file is preserved. Import does not copy provider configuration,\nplugins, sessions, or transcripts. API-key credentials are not accepted by this\nroute. Refreshed ChatGPT credentials are persisted only after the owned provider\nprocess has joined.\n\n### Connect Devin on macOS\n\nUse the exact admitted **3000.11.3** build; **3000.11.1** and **3000.10.31**\nremain admitted. All passed credential-free native boundary checks; authenticated coding\nacceptance requires separate evidence for the account, model, and build.\nModel availability is checked against the connected account's fresh catalog at\nlaunch. xcb preserves an unknown quota reset as unknown. Sign in through the\nprovider CLI, then explicitly select its\n`credentials.toml` to create a private xcb account:\n\n```sh\ndevin auth login\nxcb doctor --provider devin\nxcb accounts import-devin --source /absolute/path/to/credentials.toml\nxcb accounts refresh <account-id>\nxcb models\n```\n\nThe source file and provider sessions are preserved. xcb copies only the\ncredential for the supported provider endpoints. To update just the catalog,\nuse `xcb models refresh devin --account <account-id>`; Devin discovery requires\nan explicitly connected account. Native Devin currently uses fixed ACP model\nchoices. Adaptive and Fusion catalog representations in the compatibility\npackage do not establish native support.\n\nFor either provider, `xcb run` automatically selects an eligible model. An\noptional explicit default for direct interactive sessions can be set with a\nfull matching key from `xcb models` using `xcb models default <key>`. Select the account with\n`xcb accounts default <account>` for new direct sessions, or pass\n`--account <account> --model <key>` to `xcb run`. A provider upgrade is not\nautomatically admitted; xcb keeps routing the pinned qualified build until an\nxcb release qualifies the new artifact.\n\n### Isolated tests, builds, and Git\n\nProject commands use an explicitly provisioned Linux VM through `workspace_exec`.\nFollow the [command runner setup](https://github.com/hraness/xcb/blob/main/docs/command-runner.md#setup-and-admission)\nfrom the same source checkout as the installed native CLI. Commands run offline\nagainst a staged workspace; host dependencies, credentials, and build products\nare excluded. Native macOS and Xcode builds are unavailable. The explicit\n[public dependency preparation frontend](https://github.com/hraness/xcb/blob/main/docs/command-runner.md#dependencies-and-git)\npassed the current VM boundary suite, including rejection of a cache after its\nmanifest changed. Installed Claude and Codex coding workflows passed on macOS\nARM64 with the tested accounts; other repositories and toolchains still need\ntheir own checks.\n\nThe Git projection is limited to filtered, read-only HEAD and index data for\nstatus and diffs. Original history, remotes, and hooks are omitted; commit and\npush workflows are unavailable. Publication checks file revisions and replaces\neach file atomically; it is not a transaction across every changed file. Failed,\ncancelled, or uncertain command state is retained. Successfully published and\ndurably settled commands remove their verified input snapshot.\n\n### Application integration\n\nThe [application API](https://github.com/hraness/xcb/blob/main/docs/application-api.md) provides bounded, ephemeral\ninference with no tools or hooks. It requires evidence for the exact xcb binary,\nprovider, account and model before accepting application traffic.\n[Textbutler](https://github.com/hraness/textbutler), an MIT-licensed reference\napplication, keeps its contact access and messaging approval in its own host.\nSign-in and a successful `doctor` alone do not qualify the application route.\n\n### Everyday commands\n\n```sh\nxcb --cwd /absolute/path/to/your/project       # new control conversation\nxcb conversations                                # resumable control conversations\nxcb chat --resume <conversation-id>\nxcb tasks                                        # global managed task swarm\nxcb backlog                                      # backlog and work history across projects\nxcb attention                                    # questions, approvals and actions\nxcb schedules                                    # durable recurring prompts\nxcb projects                                     # project goals and remaining autonomy grants\nxcb memory status <conversation-id>               # explicit Wordcell binding\nxcb service status                               # opt-in macOS login startup\nxcb tasks verify <task-id>                       # verify local transition receipts\nxcb tasks messages <task-id>                     # durable cross-provider mailbox\nxcb offers --refresh                             # refresh official expiring offers\nxcb models tiers --task \"fix a race\"             # inspect Pareto layers\nxcb models route --task \"fix a race\"             # preview the eligible smart route\nxcb --cwd /absolute/path/to/your/project run --account <account-id> -p \"Explain this repository\"\nxcb sessions                                     # direct provider sessions\nxcb resume                                       # latest direct provider session\nxcb resume <session-id>\nxcb accounts\nxcb config\nxcb plugins\nxcb panes\nxcb doctor\nxcb completions zsh > /path/to/completions/_xcb\n```\n\nFor another program — typically a coding agent — `xcb --json route` is the\nclosed machine contract: one JSON task document on stdin selects an eligible\naccount/model route and runs exactly one bounded turn, returning the selected\nroute, saved session id, and settled outcome facts as bounded JSON. See\n[the route contract](https://github.com/hraness/xcb/blob/main/docs/route.md).\n\n`xcb chat --resume` reopens a control conversation; `xcb resume` opens a saved\ndirect provider session and its workspace. Neither is a headless continuation\ncommand. `/help` lists terminal commands. The [terminal guide](https://github.com/hraness/xcb/blob/main/docs/terminal.md)\ncovers editing keys, optional Vim editing, transcript search, agent guidance,\nand draft recovery.\nConversations, tasks, provider\nsessions, and credentials live in the private native state root\n`~/.local/share/xcb`; `--state /absolute/path` or `XCB_STATE` overrides it.\nThe `xcb-compat` compatibility CLI uses `~/.xcb` instead. Do not point both\nimplementations at the same state directory. `xcb --json run` includes the native session ID\nin its result so it can be reopened with `xcb resume <session-id>`.\n\nOne provider turn has a 30-minute default deadline, including initialization.\nThe `turn_timeout_ms` setting in the private state root's `config.json` accepts\n1,000–3,600,000 milliseconds (one second to 60 minutes); `xcb config` displays the\neffective configuration. Older configurations that omit it use the default.\nCancellation remains available before the deadline, and automatic continuation\nhas its own separate limits.\n\nControl conversations are concurrent and share one durable task supervisor.\nEach account still owns at most one active provider turn, and one workspace can\nhave only one admitted writer even when different accounts or terminals are\navailable. Independent workspaces and accounts can run concurrently. Managed\ncancellation may be requested from the task’s originating conversation or by an\nexplicit task ID/title elsewhere; direct provider-session cancellation remains\nowned by its terminal. Ctrl-C and SIGTERM request bounded cleanup for a headless\nrun. `xcb run` reports success only for a completed, joined, settled idle result.\n\nCancellation joins the owned process before releasing custody. If `doctor`\nreports an unsettled run, inspect `xcb recover` and the process state; an expired\nlease or a quiet terminal is not proof that the provider stopped. Recovery is\nan explicit operation, not a reason to delete state or lock files.\n\n### Fleet and remote devices\n\nEach machine can enroll as a device on a shared relay fleet. Linked machines\npublish an encrypted presence and task projection and accept fenced commands;\na controller device or another enrolled machine drives them through the same\nCLI. Remote task content stays encrypted at the device boundary.\n\n```sh\nxcb link --relay <relay-url> --email <owner email>   # enroll this machine (email one-time code)\nxcb remote admit <device>                          # admit a newly linked device\nxcb fleet                                          # devices, presence, projection staleness\nxcb dispatch <device> <workspace> -p \"<task>\"      # run a managed task on a remote machine\nxcb remote status <command-id> --wait              # wait for a posted command to settle\nxcb remote steer|cancel|answer <device> <task>     # drive a remote task\n```\n\nIds may be typed as unambiguous prefixes (`xcb remote cancel 513c t_a65b`).\nEvery remote verb is `--json`-scriptable with closed exit codes, which is the\ncontract a driving agent (for example a bot) should use. See\n[remote operations](https://github.com/hraness/xcb/blob/main/docs/remote-operations.md) for enrollment, controller\nsemantics and recovery.\n\n### Optional behavior\n\nAuto-continue and Gobstopper context management default on with bounded\ncontinuation and settled-boundary checks. Disable either with\n`xcb plugins disable auto-continue` or `xcb plugins disable gobstopper`.\nLocal usage measurement stays local. aiCharts upload is unavailable; local\nexports, external judgment, and executable hooks require separate opt-in.\nPanes are presentation data and cannot grant execution authority.\n\nThe optional judge uses TypeSafe System One (`jev-latest`) to advise routing,\ncontinuation, and context retention. It sends bounded task/response context to\nthat service; old tool-result bodies are excluded from compaction advice.\nA judge cannot qualify a provider or bypass deterministic safety gates.\nStore a key through stdin on macOS or Linux, then explicitly enable it:\n\n```sh\nxcb judge token < /secure/path/to/judge-key\nxcb judge status\nxcb judge enable\nxcb judge test\n# Later:\nxcb judge disable\nxcb judge logout\n```\n\nThe input file is an existing private credential file, not a command-line key.\nKeys are vaulted mode-0600 outside the workspace. `XCB_JEV_API_KEY` or\n`TYPESAFE_API_KEY` may supply a key via the environment. Custom endpoints require\nan explicit environment key; the vaulted key remains bound to System One.\n\n## Migrating from AgentMixer\n\nThe native command imports **one Claude credential**, preserving the source:\n\n```sh\nxcb accounts import-agentmixer --source /absolute/path/to/.agentmixer\n```\n\nThe account name comes from the observed provider identity. It does not\nmigrate transcripts or sessions. The `xcb-compat` compatibility CLI has its\nown `migrate` command and identifier changes; see the\n[compatibility migration reference](https://github.com/hraness/xcb/blob/main/docs/compatibility.md#migrating-from-agentmixer).\nDo not run compatibility migration commands against the native state root.\n\n## Standalone package\n\nThe retained TypeScript source provides host-owned routing, account custody,\nbounded tools, and provider adapters. It is separate from the native Rust app.\nFor building it locally, library examples, qualification requirements, and its\nCLI commands, see the [compatibility reference](https://github.com/hraness/xcb/blob/main/docs/compatibility.md) and\n[managed Codex contract](https://github.com/hraness/xcb/blob/main/MANAGED-CODEX.md). The\n[publishing contract](https://github.com/hraness/xcb/blob/main/docs/publishing.md) describes future verified artifacts;\nit is not evidence of a published package.\n\n## Development\n\nSee [Contributing](https://github.com/hraness/xcb/blob/main/CONTRIBUTING.md) for setup and the native, compatibility, and\nsite checks. The credential-free [native Codex boundary fixtures](https://github.com/hraness/xcb/blob/main/qualification/codex-native.md)\nand [native Devin boundary fixture](https://github.com/hraness/xcb/blob/main/qualification/devin-native.md) document\nrepeatable checks separately from authenticated live acceptance.\nRelease notes live in [CHANGELOG.md](https://github.com/hraness/xcb/blob/main/CHANGELOG.md).\nReport vulnerabilities through [Security](https://github.com/hraness/xcb/blob/main/SECURITY.md). Licensed under [MIT](https://github.com/hraness/xcb/blob/main/LICENSE).\n";
