// Generated from content/blog/*.md by scripts/sync-blog.ts. Do not edit.
export const blogStatusLabel = "Latest release: v0.8.7";
export const blogBodies: Readonly<Record<string, Readonly<{ html: string; toc: readonly Readonly<{ href: `#${string}`; label: string }>[] }>>> = {
  "introducing-xcb": {
    "html": "<p>Excalibur, written <code>xcb</code>, routes coding tasks across the Claude, Codex, and Devin subscriptions you already pay for. You give it a task. It picks one of your accounts that is signed in, idle, and not at a known quota limit, and it holds that account so no other task can use it until the provider's process has exited.</p>\n<h2 id=\"one-router-in-front-of-the-agents-you-pay-for\">One router in front of the agents you pay for</h2>\n<p>If you pay for more than one coding agent, each one comes with its own terminal, its own sign-in, its own quota, and its own idea of where a long session lives. The work ends up scattered: one account hits its limit halfway through a refactor, a second sits idle, and nobody remembers which window holds the session that was fixing the flaky test.</p>\n<p>xcb puts one router in front of those subscriptions. A task goes to an account that can take it now, long sessions are trimmed to stay inside the model's context, and every managed task leaves a local record you can replay offline.</p>\n<h2 id=\"who-xcb-is-for\">Who xcb is for</h2>\n<p>xcb is for developers who already use more than one of Claude Code, Codex, and Devin and want one workflow around them. Other agents can call <code>xcb --json route</code> with one JSON task and read one JSON result; that route picks the account and model itself. Application builders can embed the TypeScript SDK, where the application names the account and model and the router holds that account while the task runs.</p>\n<p>If you use one agent on one subscription, the provider's own tool serves you better. If you need a full replacement for Codex, Claude Code, or Devin today, xcb is not there yet, and its README says so in the first section.</p>\n<h2 id=\"starting-a-managed-conversation\">Starting a managed conversation</h2>\n<p>The native Rust app runs a terminal workspace on top of the router. With a supported Claude Code build installed, the first managed conversation looks like this:</p>\n<pre><code class=\"language-sh\">xcb accounts add claude --plan Max\nxcb doctor --provider claude\nxcb accounts login &lt;account-id&gt;\nxcb accounts refresh &lt;account-id&gt;\nxcb models\nxcb --cwd /absolute/path/to/your/project\n</code></pre>\n<p>xcb runs its own sign-in and does not silently import your existing provider login. The <code>--plan</code> flag is a display label; it does not check your subscription. Prompts in that conversation become durable tasks, and closing the terminal detaches from them without cancelling them. <code>/tasks</code> lists the work, <code>/attention</code> collects questions and approvals across agents, and <code>/steer &lt;task-id&gt; &lt;guidance&gt;</code> queues guidance for a task's next turn. Tasks in the same workspace run one at a time; independent workspaces and accounts run side by side.</p>\n<p>Native binaries are published for macOS on Apple silicon and for Linux x86_64, each with a checksum. Other hosts build from source. The <a href=\"/docs/getting-started\">getting started guide</a> covers Codex and Devin sign-in.</p>\n<h3 id=\"long-sessions-stay-inside-context\">Long sessions stay inside context</h3>\n<p>Context management is on by default. For Claude Code and Codex sessions, once a prompt passes a size threshold, xcb applies Gobstopper's elision policy to old tool output. The replaced output becomes a short marker that says how many bytes were elided and that the original is retained in local history. The eight most recent tool outputs are never elided. With the optional judge turned on, each candidate is checked first, and output the next turn still needs stays in full. The judge is an external service (TypeSafe System One) that you enable with your own key; it receives bounded task context, but not the bodies of the old tool output it rules on. <code>xcb plugins disable gobstopper</code> turns this off. The details are in <a href=\"/blog/how-xcb-uses-gobstopper\">How xcb uses Gobstopper</a>.</p>\n<h3 id=\"reflexes-act-only-after-your-replies-back-them\">Reflexes act only after your replies back them</h3>\n<p>Some decisions come up many times a day: whether a task needs a frontier model, whether a worker that stopped should keep going, whether “Should I merge it?” deserves a yes. xcb calls these reflexes, and it learns them from how you answer. When you reply “continue” to a finished turn, that labels the turn as one that stopped short. A reply of “yes, go ahead” labels a turn that asked for approval.</p>\n<p>The continuation reflexes start out observing. Since v0.6.0 they default to <code>auto</code>, which means they act only after your own replies show they are precise enough. xcb replays your labeled turns, and a reflex may act only at a threshold where it fired on at least 30 turns and the lower bound of its measured precision reaches a floor: 0.75 for continuing a stopped turn and 0.85 for answering “yes” on your behalf. About one such turn in ten is still left to you, so your replies keep measuring it, and a reflex whose precision drops goes back to observing. A request that mentions deletion, credentials or secrets, production, or spending is never answered “yes” automatically. The <a href=\"/docs/reflexes\">reflexes reference</a> documents the full rule.</p>\n<h3 id=\"task-history-you-can-replay-offline\">Task history you can replay offline</h3>\n<p>xcb uses ALGAL to record managed task transitions and to run the small planning programs behind scheduled work. Evaluating one of those programs uses no provider, subprocess, or network. A controller that needs a worker suspends at that request, xcb saves the checkpoint, and the worker starts only through the same project permissions as any other task. <code>xcb schedules program --managed-calls 2</code> lets a controller request up to two worker tasks; the limit is eight. <code>xcb tasks verify &lt;task-id&gt;</code> replays the task's local record offline. It checks that record against the current one. It cannot confirm what a provider claimed or what happened outside your machine. <a href=\"/blog/replayable-task-history\">Replayable task history</a> explains the method, and <a href=\"/blog/how-xcb-uses-algal\">How xcb uses ALGAL</a> shows where it runs.</p>\n<p>Workers can also search a Wordcell vault once you bind it to their conversation with <code>xcb memory configure &lt;conversation-id&gt;</code>. Results come back cited, and saving a note to the vault is always a separate step. See <a href=\"/blog/how-xcb-uses-wordcell\">How xcb uses Wordcell</a>.</p>\n<h2 id=\"where-xcb-is-going\">Where xcb is going</h2>\n<p>The managed harness is being rebuilt on ALGAL so that it can propose routing rules, test them on labeled examples, and keep a rule only when it scores strictly better. Provider checks, account locking, and run records stay fixed. This work is in development, and the current build does not run self-modifying routing policies.</p>\n<h2 id=\"what-it-does-not-do-yet\">What it does not do yet</h2>\n<p>xcb is not yet a daily-driver replacement for Codex, Claude Code, and Devin. Installed Claude and Codex coding workflows have been confirmed on macOS on Apple silicon with the tested accounts, and Codex support is for one exact build. The Devin builds xcb supports have passed sandbox checks run without signing in; a coding session on a signed-in Devin account has not been confirmed. The native Codex and Devin paths need macOS sandboxing, and Claude on Linux is not yet confirmed. Context trimming does not apply to Devin sessions. Project commands run in an isolated Linux VM that you set up on macOS on Apple silicon, with read-only Git status and diffs, so commit, push, and native macOS or Xcode builds are unavailable there. A reflex learns your preferences and does not measure model quality. Its precision floor is a guardrail and does not guarantee a correct call.</p>\n",
    "toc": [
      {
        "href": "#one-router-in-front-of-the-agents-you-pay-for",
        "label": "One router in front of the agents you pay for"
      },
      {
        "href": "#who-xcb-is-for",
        "label": "Who xcb is for"
      },
      {
        "href": "#starting-a-managed-conversation",
        "label": "Starting a managed conversation"
      },
      {
        "href": "#where-xcb-is-going",
        "label": "Where xcb is going"
      },
      {
        "href": "#what-it-does-not-do-yet",
        "label": "What it does not do yet"
      }
    ]
  },
  "how-xcb-uses-gobstopper": {
    "html": "<p>Two hours into a refactor, much of what your coding agent receives each turn can be old tool output: the files it read at the start, a search it has already acted on, the log from a test run it fixed an hour ago. The instructions you gave and the decisions it made are still in there, but they share the space with pages of text nobody needs again.</p>\n<p>xcb handles this for Claude Code and Codex sessions with Gobstopper. When a session grows past a threshold, xcb applies Gobstopper's elision policy to the prompt it is about to send: old tool results become a one-line marker, the recent work stays word for word, and the original output stays in xcb's local history. It is on by default. Latest release: v0.8.7.</p>\n<h2 id=\"long-sessions-fill-up-with-output-nobody-rereads\">Long sessions fill up with output nobody rereads</h2>\n<p>A coding agent works by reading. It opens files, runs searches, lists directories and runs tests, and each result lands in the conversation. The model usually needs the conclusion from that output, which it already wrote down in its own reply, and rarely needs the raw text again.</p>\n<p>That raw text still counts against the context window. As a session grows, something eventually has to be cut or summarized, and the parts worth keeping are what you asked for, what was decided and the last few results.</p>\n<h2 id=\"what-gobstopper-is\">What Gobstopper is</h2>\n<p><a href=\"https://gobstopper.sh\">Gobstopper</a> is a tool for inspecting Claude Code, Codex and Devin sessions and preparing smaller copies of them. Its simplest rule is called elide: replace old tool outputs with a short stub, oldest first, and leave the newest ones alone. It needs no model to decide what to cut, so the same session and the same settings always give the same plan.</p>\n<p>Used on its own, Gobstopper also keeps a local archive of each transcript before it prepares a compacted copy, so you can search old sessions and read back a specific archived record when a summary leaves it out.</p>\n<p>xcb uses a smaller piece: Gobstopper's core library, pinned to a specific commit in xcb's build, and its elide strategy. On this path xcb neither runs the Gobstopper program nor uses its archive; it keeps its own history.</p>\n<h2 id=\"how-xcb-applies-the-policy-each-turn\">How xcb applies the policy each turn</h2>\n<p>xcb stores every message of a session locally and builds each turn's prompt from that history. Gobstopper runs during that build, in these steps:</p>\n<ol>\n<li><strong>Estimate the size.</strong> xcb estimates the prompt at roughly one token per four bytes of text. If the estimate is below the trigger (250,000 by default), nothing changes.</li>\n<li><strong>Ask Gobstopper for a plan.</strong> xcb hands Gobstopper a list of the messages, marking only tool results as removable. Gobstopper's elide strategy walks those results from oldest to newest, skips the newest eight, and stops once the estimate would fall to the floor (40,000 by default).</li>\n<li><strong>Check it is worth doing.</strong> The plan is used only if it saves at least the minimum (4,096 estimated tokens by default).</li>\n<li><strong>Protect the recent tail.</strong> xcb separately refuses to touch any of the last eight messages, whatever their role.</li>\n<li><strong>Rewrite the outgoing copy.</strong> Each chosen tool result is replaced in the outgoing prompt by a marker that says how many bytes were removed. The saved history keeps the original.</li>\n</ol>\n<p>In outline, the rule looks like this:</p>\n<pre><code class=\"language-text\">if estimated_size &lt; trigger:\n    send the history as it is\nelse:\n    candidates = tool results, oldest first, except the newest 8\n    stub candidates until estimated_size &lt;= floor\n    use the plan only if it saves at least min_savings\n    never touch the last 8 messages, or any user or assistant text\n</code></pre>\n<p>In place of each old result, the model sees this marker:</p>\n<pre><code class=\"language-text\">[output elided by gobstopper: &lt;bytes&gt; bytes; original retained in local history]\n</code></pre>\n<p>When xcb elides anything, it tells you in the session: &quot;Gobstopper elided N stale tool outputs in the prompt; history is retained.&quot;</p>\n<p>Three rules hold at any settings. Your messages and the agent's replies are never rewritten, only tool output. The saved session is never edited; each turn starts again from the saved history. And with the judge off, the same history and settings give the same prompt, because Gobstopper's rule uses no model.</p>\n<h3 id=\"an-optional-second-opinion\">An optional second opinion</h3>\n<p>Some old results still matter, such as an exact error message or a file the agent is about to edit again. If you have turned on xcb's optional judge (off by default), xcb asks it one yes-or-no question per candidate: does the next turn still need this output word for word, where running the tool again would not do? The judge is an external judgment service, which is why it is opt-in. It sees the tool's name, the output's size, your current task and a limited excerpt of the recent conversation. It does not see the tool output itself. An output it wants to keep, with a probability of 0.5 or more, stays in the prompt.</p>\n<p>The judge can only keep things. It never adds a candidate Gobstopper did not choose. xcb asks about at most 64 candidates per turn, and any beyond that stay in full. If the judge is unavailable, fails or leaves an answer out, xcb says so in the session and falls back to Gobstopper's plan on its own. After the judge answers, xcb checks the minimum saving again and trims nothing if what remains falls below it.</p>\n<h3 id=\"tuning-or-turning-it-off\">Tuning or turning it off</h3>\n<p>The settings sit under <code>extensions.gobstopper</code> in xcb's <code>config.json</code>, and <code>xcb config</code> shows the values in effect. The defaults are:</p>\n<pre><code class=\"language-json\">{\n  &quot;extensions&quot;: {\n    &quot;gobstopper&quot;: {\n      &quot;enabled&quot;: true,\n      &quot;trigger_tokens&quot;: 250000,\n      &quot;floor_tokens&quot;: 40000,\n      &quot;min_interval_ms&quot;: 300000,\n      &quot;min_savings_tokens&quot;: 4096\n    }\n  }\n}\n</code></pre>\n<p>xcb rejects out-of-range values: for example, a floor below 1,024 or not below the trigger, or a trigger above 1,000,000. To switch the feature off, or back on:</p>\n<pre><code class=\"language-sh\">xcb plugins disable gobstopper\nxcb plugins enable gobstopper\n</code></pre>\n<h2 id=\"what-changes-for-you\">What changes for you</h2>\n<ul>\n<li><strong>Long sessions keep going without manual cleanup.</strong> Once a session crosses the trigger, stale output drops out of the prompt without you running a command or starting a fresh session.</li>\n<li><strong>What you said and what was decided stay put.</strong> Your instructions and the agent's replies go into every prompt as written, and the newest results stay in full.</li>\n<li><strong>The originals stay in local history.</strong> The saved session still has every original tool result, so the history you review, export or replay is complete.</li>\n<li><strong>The same input gives the same trim.</strong> Without the judge, the same session and settings always trim the same messages. With the judge, it can only keep more.</li>\n</ul>\n<h2 id=\"where-it-stops\">Where it stops</h2>\n<p>This covers Claude Code and Codex sessions that xcb runs. Devin sessions are sent without elision. The sizes are estimates from byte counts, not the provider's token counts, and xcb reports no measured savings or effect on your bill. A smaller prompt is also not proof that the next turn goes better; Gobstopper's own documentation makes the same point about its compaction. Once an output is elided, the model sees only the marker, so if it needs that text again it has to run the tool again. You can still read the original in xcb's history.</p>\n<p>The rest of xcb is covered in <a href=\"/blog/introducing-xcb\">Introducing xcb</a>, and Gobstopper as a standalone tool at <a href=\"https://gobstopper.sh\">gobstopper.sh</a>.</p>\n",
    "toc": [
      {
        "href": "#long-sessions-fill-up-with-output-nobody-rereads",
        "label": "Long sessions fill up with output nobody rereads"
      },
      {
        "href": "#what-gobstopper-is",
        "label": "What Gobstopper is"
      },
      {
        "href": "#how-xcb-applies-the-policy-each-turn",
        "label": "How xcb applies the policy each turn"
      },
      {
        "href": "#what-changes-for-you",
        "label": "What changes for you"
      },
      {
        "href": "#where-it-stops",
        "label": "Where it stops"
      }
    ]
  },
  "replayable-task-history": {
    "html": "<p>An agent that finishes a task hands back its own account of the work: a summary, a list of changed files, a line saying the tests passed. xcb also keeps a chain of local entries for every managed task, one for each change of state, and <code>xcb tasks verify &lt;task-id&gt;</code> replays that chain with no network. If an entry was changed after it was written, or a step in the middle is gone, the command fails and says where.</p>\n<h2 id=\"a-log-written-by-the-thing-it-describes\">A log written by the thing it describes</h2>\n<p>Software gets written quickly now. A model produces something that looks finished, survives the demo, and falls over the second time someone uses it. That is vibe-coded slop, and the common response is more agents: one to write, one to review, a scheduler to keep them working overnight. More agents means more work happens while nobody is watching, and the morning question becomes what they did.</p>\n<p>The usual answer is a log. The same program writes it, can rewrite it, and is also the one telling you everything went fine. A crash that leaves a half-written row, a bug that skips a step, and a hand edit that tidies up an awkward entry all look the same to a reader. A task list that says &quot;done&quot; with no way to check how it got there is a fragile foundation for anything built on top of it.</p>\n<h2 id=\"when-a-history-can-be-checked-by-anyone\">When a history can be checked by anyone</h2>\n<p>Suppose instead that each step writes down exactly what the task looked like at that moment, and each step points to the one before it. Later, a checker that knows nothing about the agent, the provider, or your accounts walks back from the last step to the first and redoes the bookkeeping. If everything lines up, the history on disk is the one that was written step by step. If one entry was edited or dropped, the walk stops there.</p>\n<p>That check covers the record and says nothing about the quality of the work. Once you know the record is intact, your review time can go to whether the work is right.</p>\n<h2 id=\"how-xcb-records-each-change-to-a-task\">How xcb records each change to a task</h2>\n<p>xcb stores each managed task as a record with a revision number. Every change of state, such as pausing for your answer or accepting your reply, produces a new revision. Before a revision is saved, xcb passes it through a one-step program on the ALGAL runtime whose only job is to accept the record as given. ALGAL produces an entry holding the input, the output, a fingerprint of that program, and a fingerprint of the entry itself, and xcb stores that entry next to the task.</p>\n<p>A fingerprint here is a SHA-256 digest of a canonical encoding of the data, so changing any field changes it. The stored task keeps the fingerprint of its latest entry. The record inside each entry carries the fingerprint of the entry before it, and the first revision points at a fixed placeholder, <code>sha256:pending</code>, instead.</p>\n<pre><code class=\"language-text\">stored task   latest = fp(entry 3)\nentry 3       revision 3, previous = fp(entry 2)\nentry 2       revision 2, previous = fp(entry 1)\nentry 1       revision 1, previous = &quot;sha256:pending&quot;\n</code></pre>\n<h2 id=\"the-rules-the-check-enforces\">The rules the check enforces</h2>\n<p>Starting from the stored task, the verifier walks backward and requires four things at every step.</p>\n<ol>\n<li><strong>Replay reproduces the entry.</strong> ALGAL reruns the entry against an empty store with no host tools, and the result must match what was written. Nothing in this step calls a provider or the network.</li>\n<li><strong>The entry matches the task.</strong> The record inside the entry must equal the task at that revision, field for field, apart from the pointer to the entry itself.</li>\n<li><strong>No gaps.</strong> Each earlier entry has a revision exactly one lower and the same task identity, and the walk must end at revision 1, whose predecessor is the placeholder.</li>\n<li><strong>The same rules made every entry.</strong> Each entry's program fingerprint must match the one stored on the task, and replay uses the transition program compiled into your xcb build.</li>\n</ol>\n<p>In outline, written for this post rather than copied from the source:</p>\n<pre><code class=\"language-python\">def verify(task):\n    expected, replayed = task, 0\n    while True:\n        entry = find_entry(task.id, expected.revision, expected.latest)\n        if entry is None:\n            fail(&quot;a revision is missing&quot;)\n        if not replay_matches(entry) or entry.program != task.program:\n            fail(&quot;replay does not match&quot;)\n        if entry.record != expected:          # ignoring the self-pointer\n            fail(&quot;entry does not match the stored task&quot;)\n        replayed += 1\n        if expected.revision == 1:\n            if entry.record.previous != &quot;sha256:pending&quot;:\n                fail(&quot;history does not start at the beginning&quot;)\n            return {&quot;verified&quot;: True, &quot;revisions&quot;: replayed}\n        expected = previous_record(entry)     # revision - 1, same identity\n</code></pre>\n<p>Running it prints one JSON object:</p>\n<pre><code class=\"language-sh\">xcb tasks verify &lt;task-id&gt;\n</code></pre>\n<pre><code class=\"language-json\">{&quot;taskId&quot;: &quot;&lt;task-id&gt;&quot;, &quot;verified&quot;: true, &quot;revisions&quot;: 3,\n &quot;receipt&quot;: &quot;sha256:...&quot;, &quot;policy&quot;: &quot;sha256:...&quot;}\n</code></pre>\n<p><code>revisions</code> counts the entries replayed. <code>receipt</code> is the fingerprint of the latest entry and <code>policy</code> is the fingerprint of the transition program. Any failure exits with an error instead of this object.</p>\n<h2 id=\"what-the-tests-hold-it-to\">What the tests hold it to</h2>\n<p>xcb's test suite pins the behavior with real task histories. In one test, a task is created, pauses for input, and gets an answer, and the verifier replays three revisions. The test then rewrites the stored task's goal to &quot;different task&quot; directly in the database, and verification fails. Another test rolls a state database back to an older schema, reopens it so xcb upgrades it, and checks that the existing task still verifies.</p>\n<p>Scheduled and backlog programs get the same treatment at a different point. xcb pins a planner program's content and inputs by fingerprint and re-checks both before each step. A saved checkpoint that was changed cannot resume, even when whoever changed it recomputes the checkpoint's own fingerprint, because the checkpoint must still agree with the pinned program. The tests try this with changed inputs, a swapped executor fingerprint, a changed request fingerprint, and an edited step record.</p>\n<p>Automatic continuation reads the same task state. A contract test holds that automatic continuation never fires for a turn whose outcome xcb could not confirm, a cancelled turn, a completed turn, or one waiting for your attention.</p>\n<h2 id=\"checking-a-copy-on-another-machine\">Checking a copy on another machine</h2>\n<p>Every xcb command accepts <code>--state &lt;dir&gt;</code> to use a state directory other than the default <code>~/.local/share/xcb</code>, so you can copy a state directory to a machine you trust and run the check there:</p>\n<pre><code class=\"language-sh\">xcb --state /path/to/copied-state tasks verify &lt;task-id&gt;\n</code></pre>\n<p>Opening a state directory applies xcb's normal schema upgrades and, at most once a day, its 30-day retention pass, so point it at a copy you can afford to change, not the only one.</p>\n<h2 id=\"what-a-passing-check-does-not-tell-you\">What a passing check does not tell you</h2>\n<p>A verified history says the entries on disk are complete and unchanged since xcb wrote them, one step at a time. It does not say the agent's work is right. The check does not read the code, rerun the tests, or ask the provider what happened, and xcb's README states the same limit: the check does not vouch for provider claims or real-world outcomes.</p>\n<p>The check compares entries with each other and with the stored task. Nothing is signed, and the entries live in your local state directory, so someone with write access who rebuilds every entry and every fingerprint consistently would produce a history that passes. What it catches is an edited entry, a missing step, or a half-written row.</p>\n<p>The verifier refuses a history longer than 1,024 revisions. Finished tasks older than the 30-day retention window can be deleted together with their history (tasks that other live work still refers to are kept), so check a task before then if you want to keep the result. The managed harness that records these histories is experimental.</p>\n",
    "toc": [
      {
        "href": "#a-log-written-by-the-thing-it-describes",
        "label": "A log written by the thing it describes"
      },
      {
        "href": "#when-a-history-can-be-checked-by-anyone",
        "label": "When a history can be checked by anyone"
      },
      {
        "href": "#how-xcb-records-each-change-to-a-task",
        "label": "How xcb records each change to a task"
      },
      {
        "href": "#the-rules-the-check-enforces",
        "label": "The rules the check enforces"
      },
      {
        "href": "#what-the-tests-hold-it-to",
        "label": "What the tests hold it to"
      },
      {
        "href": "#checking-a-copy-on-another-machine",
        "label": "Checking a copy on another machine"
      },
      {
        "href": "#what-a-passing-check-does-not-tell-you",
        "label": "What a passing check does not tell you"
      }
    ]
  },
  "how-xcb-uses-algal": {
    "html": "<p>A coding agent finishes a turn with &quot;Pushed; waiting on CI.&quot; Another ends with &quot;Should I merge it?&quot; You answer the first with &quot;continue&quot; and the second with &quot;yes, go ahead&quot;, and by the fortieth time that day you would happily let something answer for you. The catch is the forty-first turn, where &quot;go ahead&quot; would have deleted a branch or shipped to production. xcb handles these small, repeated calls with reflexes, and it runs each reflex as an ALGAL program so the reflex has to prove itself on your own history before it acts.</p>\n<h2 id=\"fewer-interruptions-and-no-guesses-on-risky-turns\">Fewer interruptions, and no guesses on risky turns</h2>\n<p>Someone who routes work through xcb wants fewer interruptions. A turn that clearly stopped short should keep going without a typed &quot;continue&quot;. A request for a harmless next step should get its &quot;yes&quot;. A new task that needs a frontier model should get one, and a quick question should not.</p>\n<p>They also want limits on that help. A shortcut learned last week should stay out of risky requests, act only where your history shows it is right, and leave a record anyone can check afterward. xcb keeps the part that learns small and runs it where every decision can be recomputed.</p>\n<h2 id=\"what-algal-does\">What ALGAL does</h2>\n<p><a href=\"https://algal.computer\">ALGAL</a> is a language and application VM for bounded agent programs. An ALGAL program, which ALGAL calls an organism, is written as data: the steps, how they connect, and the limits each run must respect, such as how many steps it may take and how many model calls it may make. The host application decides what the program is allowed to touch. When a program runs, it leaves a record of what it did, so someone can explain the result later without calling a model again.</p>\n<p>xcb embeds the Rust ALGAL runtime directly, pinned to one exact commit of the ALGAL repository, so an ALGAL update never changes xcb's behavior until xcb itself moves the pin.</p>\n<h2 id=\"how-xcb-runs-its-reflexes-on-algal\">How xcb runs its reflexes on ALGAL</h2>\n<p>xcb ships two reflexes. The route reflex picks a frontier or standard model tier for a new task. The settle reflex reads how a worker's turn ended and has two heads: one decides whether the turn stopped short and should continue, and one decides whether the turn is asking for a go-ahead that xcb may answer.</p>\n<p>Every reflex decision has the same shape:</p>\n<pre><code>decision = program(features, params, evidence)\n</code></pre>\n<p>The program is an ALGAL organism that does arithmetic and nothing else. It has no model calls and no side effects, and its declared limits say so. A simplified sketch of the shipped settle program looks like this:</p>\n<pre><code class=\"language-json\">{\n  &quot;contract&quot;: &quot;algal.organism.v1&quot;,\n  &quot;budgets&quot;: { &quot;maxAgentCalls&quot;: 0, &quot;maxSteps&quot;: 6 },\n  &quot;cells&quot;: [\n    { &quot;id&quot;: &quot;src&quot;, &quot;kind&quot;: &quot;input&quot; },\n    { &quot;id&quot;: &quot;head&quot;, &quot;kind&quot;: &quot;expr&quot; },\n    { &quot;id&quot;: &quot;decide&quot;, &quot;kind&quot;: &quot;expr&quot; }\n  ]\n}\n</code></pre>\n<p>The features are numbers xcb computes from the end of the worker's report: how many tool calls the turn made, whether the last paragraph reads as still in progress, whether it asks a question, whether it names a risky step. The params are one small scoring model per head, with a threshold. Learning adds a new version of those params. It never edits the program.</p>\n<p>That split gives xcb two rules:</p>\n<ol>\n<li><strong>Same inputs, same answer.</strong> The same program, params, features, and evidence always give the same decision and the same run fingerprint. xcb records the program's fingerprint and the params version with every observation, so any past decision can be run again and must come out identical.</li>\n<li><strong>The program cannot widen its own permissions.</strong> You can replace a reflex program with your own, but xcb accepts it only if it has no effectful steps and no model calls. The veto on risky requests lives in xcb itself, not in the replaceable program, so a custom program cannot remove it.</li>\n</ol>\n<h3 id=\"when-a-head-may-act\">When a head may act</h3>\n<p>The settle heads default to a mode xcb calls <code>auto</code>:</p>\n<pre><code class=\"language-json\">{ &quot;route&quot;: &quot;active&quot;, &quot;settle&quot;: &quot;auto&quot;, &quot;confirm&quot;: &quot;auto&quot;, &quot;learn&quot;: true }\n</code></pre>\n<p>In <code>auto</code>, a head starts out only watching. Your replies label its decisions: &quot;continue&quot; or &quot;push it&quot; after a finished turn marks it as stopped short, &quot;yes&quot; or &quot;lgtm&quot; marks a turn that asked for approval, &quot;wait&quot; or &quot;no, ...&quot; marks a miss.</p>\n<p>After training passes and imports, xcb replays your labeled turns from the shipped starting point, so each turn is scored by a version of the head that had not yet learned from it. Only your own labels count toward this. Labels from turns xcb answered itself train the head but cannot certify it, because they would only confirm its own choices. On the newest 1,500 of your labeled turns (for the confirm head, only requests it could answer, with no risk or hand-off cue), a head is certified at the lowest threshold where it fired at least 30 times and a conservative 99% lower bound on its precision reaches a floor: 0.75 for continuing a stopped turn, and 0.85 for answering &quot;yes&quot;, since saying yes on your behalf is the bigger step.</p>\n<p>Once certified, a head acts only on turns it scores at or above that threshold. About one such turn in ten, chosen by a hash of the task and turn, is still left for you. Those replies are the only unbiased evidence a head keeps getting after it starts acting, and they are what can take its certificate away. If measured precision falls more than 0.05 below the floor, the head goes back to watching. You can also set a head to <code>observe</code> so it never acts, or to <code>active</code> so it acts without a certificate.</p>\n<p>Even a certified confirm head never says &quot;yes&quot; when the turn hands something to you, when its report mentions deletion, secrets, production, or spending, or when the request proposes a deploy, release, tag, migration, removal, payment, access change, or message to people. A configured judge can still veto what is left.</p>\n<p>The route reflex works differently. It is <code>active</code> by default, and its starting version reproduces how xcb chose tiers before reflexes existed. It only ever chooses between routes that were already eligible, and a long prompt's minimum quality tier cannot be lowered.</p>\n<h2 id=\"what-you-get\">What you get</h2>\n<p>Fewer &quot;continue&quot; and &quot;yes&quot; messages on the turns where your own history says xcb gets them right, and none on the turns where it has not shown that yet. The reflexes reference reports a check on one operator's private history of 2,428 follow-up messages: the continuation head cleared its floor on the largest time-ordered split, and the confirm head never fired often enough to reach its 0.85 floor. On that history, <code>auto</code> would continue stopped-short turns once enough fresh replies accumulate, and leave every go-ahead request to that person.</p>\n<p>You can inspect what each head did. <code>xcb reflex status settle</code> shows each certificate and the evidence behind it. <code>xcb reflex import settle history.jsonl --dry-run</code> replays a history you supply and shows whether it alone would certify each head, without storing anything. <code>xcb reflex rollback settle 0</code> puts the shipped starting version back. The local ledger keeps numeric features, decisions, labels, and fingerprints, and never the text of a prompt or response.</p>\n<p>The same runtime also records managed task transitions and runs the small resumable controllers behind scheduled work, which is what lets <code>xcb tasks verify</code> replay the chain of records a task left on your machine and check it against the stored record. <a href=\"/blog/replayable-task-history\">Replayable task history</a> covers that side.</p>\n<h2 id=\"limits\">Limits</h2>\n<p>xcb's README calls the managed harness experimental, and the reflexes are part of it. A reflex learns one person's preferences and does not measure model quality, so a learned route predicts the tier you would pick. The 0.75 and 0.85 floors hold only approximately: up to seven thresholds are tried and the test repeats as labels arrive, so the real chance of certifying a head below its floor is somewhat higher than 1%, and once a head acts, a real decline shows up only after a few hundred of the replies left to you. Replaying a decision confirms that xcb's recorded inputs produce the recorded answer. It does not confirm that the answer was the right call for your work.</p>\n<p>For the rest of the product, start with <a href=\"/blog/introducing-xcb\">Introducing xcb</a>. The full rules, including every reply category and the continuation checks, are in the <a href=\"/docs/reflexes\">reflexes reference</a>.</p>\n",
    "toc": [
      {
        "href": "#fewer-interruptions-and-no-guesses-on-risky-turns",
        "label": "Fewer interruptions, and no guesses on risky turns"
      },
      {
        "href": "#what-algal-does",
        "label": "What ALGAL does"
      },
      {
        "href": "#how-xcb-runs-its-reflexes-on-algal",
        "label": "How xcb runs its reflexes on ALGAL"
      },
      {
        "href": "#what-you-get",
        "label": "What you get"
      },
      {
        "href": "#limits",
        "label": "Limits"
      }
    ]
  },
  "how-xcb-uses-wordcell": {
    "html": "<p>Last month you wrote down why the parser gives up after three retries. The note sits in a Markdown folder, next to the reasons you chose one test runner over another and the list of commands that must never run on the release branch. Today an agent is about to change the parser, and it has never seen any of it.</p>\n<h2 id=\"your-notes-are-where-the-decisions-live\">Your notes are where the decisions live</h2>\n<p>A coding agent starts every task knowing the code and nothing else. The reasons behind the code live in a notes folder, an Obsidian vault or a plans directory, so the agent either rediscovers a decision the hard way or undoes it. Pasting notes into each prompt works until the pasted copy goes stale, and then nobody can tell which note a claim came from.</p>\n<p>If you run your agents through <a href=\"/\">xcb</a>, you can point a project at that folder once. Its agents can then search your notes while they work, and every result names the note it came from, so you and the agent can open the file and check.</p>\n<h2 id=\"wordcell-for-someone-who-has-never-used-it\">Wordcell, for someone who has never used it</h2>\n<p><a href=\"https://wordcell.io\">Wordcell</a> is a command-line tool for a knowledge base kept as ordinary Markdown files. Your files stay the record. Wordcell adds search and a link graph on top of them, and recent versions can search an existing folder or Obsidian vault without converting it. Its exact search mode reads the current Markdown directly, with no model download and no network request, and each hit comes back with the path of the note that matched.</p>\n<h2 id=\"how-xcb-wires-a-project-to-one-vault\">How xcb wires a project to one vault</h2>\n<p>xcb never finds or connects a vault on its own. You bind one vault and one installed copy of Wordcell to a project conversation, check the binding, and from then on the project's workers can search it:</p>\n<pre><code class=\"language-sh\">xcb memory configure &lt;conversation-id&gt; --vault /absolute/project/vault --wordcell /absolute/bin/wordcell\nxcb memory status &lt;conversation-id&gt;\nxcb memory search &lt;conversation-id&gt; &quot;parser decision&quot;\nxcb memory promote &lt;task-id&gt; --body-file decision.md\n</code></pre>\n<p>The last line saves a note back, covered below. The TUI offers the same search as <code>/memory search &lt;query&gt;</code>.</p>\n<h3 id=\"the-binding-is-pinned\">The binding is pinned</h3>\n<p>When you bind, xcb records a SHA-256 fingerprint of the Wordcell program you named. If that program is a script, xcb fingerprints its interpreter too. For Wordcell's standard launcher, which runs through Bun, xcb looks Bun up on your <code>PATH</code> once, at binding time, and from then on starts that exact Bun directly, so a later change to your <code>PATH</code> cannot swap it. The vault has to be a directory you own that neither your group nor other users can write to, and xcb remembers which directory it is on disk, not only its path.</p>\n<p>Before every search or save, xcb checks all of that again. If the Wordcell program changed, or the folder at that path is now a different folder, the call fails until you bind again. Replacing a binding requires the current binding's revision number, so two edits cannot overwrite each other unnoticed.</p>\n<h3 id=\"what-a-worker-can-ask-for\">What a worker can ask for</h3>\n<p>Inside a managed project, a worker gets one tool for your notes, <code>xcb_memory_search</code>. It takes a query and an optional result count, and nothing else:</p>\n<pre><code class=\"language-json\">{ &quot;query&quot;: &quot;parser retries&quot;, &quot;limit&quot;: 8 }\n</code></pre>\n<p>The query can be up to 1,024 bytes, and the count runs from 1 to 16, with 8 as the default. There is no argument for choosing a vault or writing a note. The tool's own description tells the agent that retrieved text is historical, untrusted context and that facts which change over time need checking again.</p>\n<p>Behind the tool, xcb runs Wordcell in exact mode with Git history and graph expansion turned off, asks for JSON, and starts the process with a cleared environment that holds only a minimal system <code>PATH</code> and a few fixed settings, with the vault as its working directory. A query that starts with a dash is refused, so a search can never be read as a command-line option. The search has 15 seconds and 64 KiB of output; if it runs over either, xcb stops the process and everything it started, and returns an error instead of a partial answer.</p>\n<p>The worker gets Wordcell's result as JSON, including the note path for each hit. Nothing from the vault is added to a worker's prompt unless the worker asks. Fresh workers do receive a short list of recent task summaries from the same project, and xcb labels that list as its own working memory, separate from your Wordcell notes.</p>\n<h3 id=\"saving-a-note-back-is-always-your-step\">Saving a note back is always your step</h3>\n<p>xcb gives workers no tool for writing to the vault; its memory tools only read. (A worker's ordinary file tools work inside the project's workspace, so keep the vault outside that workspace if you want the separation to hold.) To keep something a task learned, you write a short note yourself and save it against that task:</p>\n<pre><code class=\"language-sh\">xcb memory promote &lt;task-id&gt; --body-file decision.md\n</code></pre>\n<p>The note can be up to 8 KiB of UTF-8. xcb never exports a conversation or copies a task summary on its own. It appends a short source block naming the xcb conversation, the task, and an ID for this save request, then asks Wordcell to create the note, tagged <code>xcb</code>, at the top of the vault, with a name derived from a hash of the note, its source task and conversation, and the binding. The saved file ends like this:</p>\n<pre><code class=\"language-md\">Parser retries stop after three attempts.\n\n---\nSource: xcb conversation `&lt;conversation-id&gt;`, task `&lt;task-id&gt;`.\nPromotion request: `&lt;request-id&gt;`.\n</code></pre>\n<p>Because the name comes from that hash, saving the same note for the same task twice leaves one note, and Wordcell refuses to overwrite a file that already exists. xcb writes down what it is about to do before it starts Wordcell, and it reports success only when Wordcell answers with the expected note path and a SHA-256 revision. A timeout, a crash, or an unclear answer is reported as uncertain, and any result short of a confirmed save makes the command exit with a nonzero code, so a script cannot mistake it for a save.</p>\n<h2 id=\"what-you-get-as-an-xcb-user\">What you get as an xcb user</h2>\n<p>Your agents can find the decision you already made, in your words, and show you where it lives. You can open that note, correct it, or delete it, and the next search sees the change, because Wordcell reads the live files. Your notes stay in your own folder, in Markdown, under whatever version control you already use. A note is added only when you save one, and it names the task that produced it.</p>\n<h2 id=\"limits\">Limits</h2>\n<p>Search through xcb is exact matching only: words, phrases, titles, tags, and paths that actually appear in your notes. Wordcell's semantic and hybrid (meaning-based), keyword-index, graph-expansion, Git-history, and hosted reranking options are not available through xcb. Each project binds one vault, and the search tool belongs to managed project workers; direct sessions do not get it. The fingerprint covers the Wordcell launcher and its interpreter, not every file in the installed Wordcell package, so you are trusting the copy of Wordcell you installed. A result is what a note said when someone wrote it, not proof that it is still true; the agent is told to check facts that change. Latest release: v0.8.7.</p>\n<p>To go further, read <a href=\"/blog/introducing-xcb\">Introducing xcb</a> for the router itself, <a href=\"https://wordcell.io/blog/introducing-wordcell\">Introducing Wordcell</a> for the knowledge base, or <a href=\"/blog/replayable-task-history\">how to check an xcb task history offline</a> for the task records a saved note points back to.</p>\n",
    "toc": [
      {
        "href": "#your-notes-are-where-the-decisions-live",
        "label": "Your notes are where the decisions live"
      },
      {
        "href": "#wordcell-for-someone-who-has-never-used-it",
        "label": "Wordcell, for someone who has never used it"
      },
      {
        "href": "#how-xcb-wires-a-project-to-one-vault",
        "label": "How xcb wires a project to one vault"
      },
      {
        "href": "#what-you-get-as-an-xcb-user",
        "label": "What you get as an xcb user"
      },
      {
        "href": "#limits",
        "label": "Limits"
      }
    ]
  }
};
